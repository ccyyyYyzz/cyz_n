# Decision 0005 — adopt the repaired uniform visible-structure theorem

Date: 2026-07-22

## Decision

Response 0005 repairs the three blocking interfaces of response 0004 and is
adopted subject to the local corrections below.

The raw response fiber is

\[
\mathfrak C_D=
\{C:\operatorname{Pred}(C)\in D,\ C\text{ obeys only predeclared finite
complexity, height and order bounds}\}/\mathcal G.
\]

Candidate truth \(\mathsf T(C)\), route success \(\mathsf R(C,D)\) and the
three external completeness artifacts are distinct. Missing evidence cannot
be converted into target falsity. An empty raw fiber reports declared
model-class/data incompatibility, not a non-\(3+1\) world.

At a regular physical character, multiplication supplies

\[
\mu_{2,x}:\operatorname{Sym}^2T_x^*B_0\xrightarrow{\sim}K_x,
\]

and the reduced Green equation proves with the algebraic dual

\[
H_x^\vee(p_{L,x})=\beta_x.
\]

For centered sources on a four-dimensional regular germ, rank nine plus an
independent nonzero-principal witness therefore identifies the actual
co-metric line

\[
\mathcal L_x=\mu_{2,x}^\vee(\ker H_x^\vee)
\subset\operatorname{Sym}^2T_xB_0.
\]

For every scoped matrix-valued symbol, the adopted strong common-cone
condition is

\[
\mathsf P=q\otimes K,\qquad K\in GL(F),
\]

uniformly over every nuisance-compatible feasible symbol. A nominal
invertible factor or determinant cone is insufficient. Complete tangent,
symmetric-square, filtered-jet and time-component cocycles remain mandatory.

## Required local corrections

1. Theorem 6 is read as a theorem about the **strong fixed-cone class**:
   every feasible symbol belongs to
   \(\{q\otimes K:K\in GL(F)\}\) iff both off-line and singular-factor
   witness sets are empty. It then follows that the characteristic set is
   \(q=0\). Equality of determinant singular sets alone does not imply strong
   factorization; a Jordan symbol is the counterexample.
2. The exact positive control may not use the scalar datum
   \(G(z_0^2)(0)=-2\) alone as evidence that the principal functional is
   nonzero. It must register a complete two-jet Green matrix, verify
   invertibility, form \(L=G^{-1}\), and directly check
   \(p_L|_{K_0}\ne0\).
3. The inequivalent-carrier adversary must use two four-dimensional regular
   carriers, for example circle/hyperbola products with \(\mathbb R^3\), if
   both candidates are claimed to satisfy target truth.
4. Noisy null-directed records require an explicit positive component
   separation margin.

## Evidence boundary

Adopted as exact mathematics: verdict logic, Green--\(\mu_2\) soldering,
rank-nine line theorem, strong fixed-\(q\) criterion, compact-set uniform
invertibility equivalence, and complete overlap/time cocycle theorems.

Still conditional: effective ideal-membership/Positivstellensatz bounds,
compact gauge slices and independently supplied context, relation/carrier,
physical-coverage and polarization-completeness artifacts.

Still implementation architecture: exhaustive raw-fiber construction,
certified global semialgebraic optimization, interval propagation through the
whole chain and scalable full-structure orbit emptiness.

## Program consequence

The former Paper I/II/III labels are now internal work packages only. The
project will be treated as one closed loop: anonymous reconstruction, an
independent scale-functorial dimension witness, dynamical selection of a
stable visible \(3+1\) package, and a falsifiable loss-of-plateau prediction.
No separate submission is planned while this loop is open.
