---
brief: 0014
source: browser-hosted GPT
captured: 2026-07-23
repository_commit: c0ad3f7a8dfa2633f426825888457684d3c0b942
status: proposed
artifacts:
  - artifacts/0014/generate_0014.py
  - artifacts/0014/verify_0014.py
  - artifacts/0014/verify_0014_payload_0.b85
  - artifacts/0014/verify_0014_payload_1.b85
  - artifacts/0014/framewise_scheduled_kernel.json
  - artifacts/0014/hostile_mutation_report.json
---

# Response 0014 — closed framewise scheduled kernel with an independent hostile verifier

## Executive result

The scheduled scaffold is now closed as an actual finite stochastic object upstream of every response-rank classifier. It contains no microscopic dimension, active mask, preferred subset, response band, visible count, or target-rank input, and it makes no rank-three selection or exclusion claim.

The delivered object is a deterministic-age, scheduled Markov-renewal kernel, not a CTMC. For every serialized source state it has exactly one physical schedule and one exact rational stochastic row. Every positive atom contains its source, scheduled hold, physical time unit, frame-local mark, conditional channel, destination, reverse event or cemetery exemption, system/reservoir/work energy changes, nine-component charge change, physical-projection flag, history flag, and source-registry provenance.

For the full rectangular `T9` calibration, independent expansion gives:

| quantity | exact count |
|---|---:|
| ordered distinct frames | 504 |
| frame-local marks | 6,556 |
| states | 21,172 |
| stochastic rows | 21,172 |
| unique schedules | 21,172 |
| event atoms | 78,628 |
| positive non-null atoms | 39,312 |
| positive non-null atoms with valid reverse | 39,312 |
| dangling state/mark/destination/reverse IDs | 0 |
| minimum and maximum row sum | `1/1` |

The defect in Response 0013 is repaired literally. Event rows are generated from each source state’s actual ordered frame. In particular, `f013=(0,1,3)` uses the normal axes `{2,4,5,6,7,8}` and includes `axis_2_plus/minus`; it does not reuse absolute `f012` templates. Every products state has a reverse-creation row.

The GKM calibration uses the registered deterministic schedule

\[
t_n=n\,\frac{r}{\bar v}.
\]

With `r=1` and `vbar=1/2`, independent trajectory replay returns exactly

\[
2,4,6,8,10
\]

in `2*pi*sqrt(alpha_prime)/c`, with zero waiting-time variance. A Poisson/exponential tag is rejected.

The same registered dependency program is applied without modification to full nine-component metric inputs representing the three-large and four-large source controls. The constructor never receives either cardinality. Complete expansion gives:

| input | valid frames | states | atoms | positive non-null/reverse pairs |
|---|---:|---:|---:|---:|
| full `T9` | 504 | 21,172 | 78,628 | 39,312 |
| three-large source control | 6 | 1,534 | 1,570 | 36 |
| four-large source control | 24 | 1,732 | 2,308 | 432 |

The independent verifier also expands distinct velocity-, coupling/dilution-, geometry-, and amplitude-invalid instances. Each has 1,516 states and 1,516 rows, and all present states route with probability one to the appropriate absorbing cemetery.

All 22 hostile mutations were run in isolated subprocesses. Every child returned exit code 2 and the intended localized semantic code. The generator, verifier, compact artifact, CRLF replay, exhaustive expansion, and mutation report were actually executed before publication.

> **Framewise scheduled-kernel theorem.** The registered dependency program and compact payload have a unique finite expansion under the serialized schema. That expansion is a target-neutral, age-augmented scheduled Markov-renewal kernel with exact stochastic closure, frame-local marks, complete destination and reverse closure, exact energy/work/charge ledgers, executable source-validity cemeteries, a fixed exchangeable initial law, exhaustive adjacent-`S9` covariance on states, marks, schedules and event atoms, and complete same-program three/four source controls. A separately implemented verifier imports no generator code, regenerates the canonical payload, independently expands the process, and rejects every mandated hostile mutation for a localized semantic reason.

This closes outcome 1 of Brief 0014 in the declared **GKM-scheduled, proposed sparse impact closure**. It is not a visible-dimension theorem.

---

## 1. Evidence boundary

### `primary-source derived`

The source-authorized pieces are the GKM heavy/dilute impact probability and scheduled-return prescription `t_r≈r/vbar`; JJP crossing-conditioned channels rather than an absolute crossing clock; DFM source-regime constraints; the EGJK anisotropic nine-torus source setting; and the rule that a winding reaction changes occupations/reservoir energy but not geometry, dimension, response relations, or a projector.

### `new proposed measurable closure`

The sparse impact preparation is central plus signed basis marks on the locally computed normal axes. The reservoir reverse ratio is `1/4`; history is the finite alphabet `none/central/shell`; the initial law is uniform positive on all noncemetery event-epoch states; and the constitutive program is the registered finite DSL below. The artifact intentionally uses the narrower label `GKM-scheduled, proposed sparse impact closure` rather than claiming a faithful cubature of the full continuous GKM law.

### `exact finite-model theorem`

Exact claims are limited to the generated finite object: row normalization, state/schedule coverage, atom closure, reverse involution, ledger closure, deterministic age replay, source cemeteries, `S9` covariance, same-law source controls, canonical regeneration, CRLF robustness, and hostile-mutation rejection.

The only transcendental input is a shell factor proportional to `exp(-1/4)`. The generator stores a rational point inside a rigorous outward interval; all exported probabilities and ledgers are exact fractions.

---

## 2. Registered non-encoding program

The constructor consumes only frozen pre-classifier leaves:

```text
all_directions, instance_id, frame_axes, frame_id,
metric_radii, self_dual_radius, separation_fraction,
relative_speed, speed_min, speed_max, coupling, coupling_max,
dilute_flag, amplitude_status, history, state_class,
source_state_id, mark_template, mark_class, mark_b2,
reservoir_reverse_ratio
```

No function receives a response cell, pole band, visible count, active mask, dimension, or target rank. The graph is topologically ordered; each node input is a whitelisted leaf or preceding node; each operation has a closed arity and parameter schema. Environment/file/network reads, free-form rules, unregistered globals, classifier lookup, and post-classifier overrides are forbidden.

The registered program hash is

```text
a979ba2d545b9333ac9178a0e786763f28f119f43f3ea57c8a344f3a06403f6c
```

For a frame `f=(w,v,s)`, it computes

```text
velocity_axis   = frame_axes[1]
separation      = metric_radii[velocity_axis] * separation_fraction
scheduled_hold  = separation / relative_speed
normal_axes     = all_directions \ frame_axes
impact_axes     = local normal axes with radius > self_dual_radius
marks           = central plus signed basis marks on impact_axes
```

Only afterwards does it look up the source-valid conditional annihilation probability and construct miss/reverse probabilities and destinations. It never computes the number of long radii.

The verifier structurally rejects all six hidden-three attacks required by the brief: explicit `count_if==3`, a hidden branch in fraction arithmetic, a count alias, a special-three registry lookup, an environment/global read, and a post-construction response-rank override. These attacks are rejected by DSL operation, parameter-schema, leaf-whitelist, or registry-provenance checks—not by token matching alone.

---

## 3. Exact finite kernel

There are 504 ordered frames, all permutations of three distinct elements of `{0,...,8}`. Each frame has three present states, one for each history class. A valid frame also has one products state for every `(history, local mark)` pair. Every instance contains four absorbing cemetery states: `velocity`, `coupling_dilution`, `geometry`, and `unresolved_amplitude`.

For a valid present state, each local mark supplies two atoms:

1. `annihilate`, accepted and non-null, moving to the corresponding products state;
2. `miss`, null/history-only, moving to the present state with updated history.

The exact row probability is

\[
P(m)\,p_{\rm ann}(m)+P(m)\,[1-p_{\rm ann}(m)]
\]

summed over all local marks, hence exactly one.

For a products state, the row is

1. `reverse_create`, accepted and non-null, using the time-reversed mark and returning to the matching present state;
2. `reverse_idle`, null, remaining in the same products state.

Every positive annihilation atom and reverse-creation atom point to one another. Their sources/destinations are swapped, marks are time reversed, channel kinds are compatible, and energy and charge vectors are opposite.

The system pair energy is two exact units. Forward annihilation has

```text
delta_system_energy    = -2
delta_reservoir_energy = +2
delta_work_energy      = 0
delta_global_charge    = zero vector in Z^9
```

and reverse creation has the opposite energy changes. Miss/idle/history atoms have zero ledgers and `physical_projection_changed=false`. The verifier derives expected ledger changes from source and destination state fields; it does not compare against a hard-coded summary.

Invalid source states retain their local schedule but have a single probability-one `source_invalid` atom into the corresponding absorbing cemetery. Cemetery states have exact absorbing self rows and an explicit schedule exemption.

---

## 4. Exact scheduled age semantics

Every noncemetery row contains

```text
clock_semantics = deterministic_scheduled
scheduled_hold_time
physical_time_unit = 2*pi*sqrt(alpha_prime)/c
initial_countdown_phase = 0
phase_count = 4
event_at_phase = 4
phase_step = scheduled_hold_time/4
```

The age-augmented trajectory advances phase deterministically and applies the row only at the event phase. The exported object is therefore semi-Markov/event-scheduled and is explicitly `not_a_ctmc=true`. Altering one hold produces the wrong calibration sequence; replacing the tag by Poisson produces a distinct verifier failure.

---

## 5. Full-process `S9` certificate

The artifact serializes the eight adjacent transpositions. The independent verifier expands their action on directions, ordered frames, states, local marks, schedules, event IDs, destinations, reverse IDs, history labels, and nine-component ledger vectors.

For the full object it verifies bijection and involution, nonadjacent commutation, adjacent braid relations, nontriviality, exact probability and schedule covariance, destination/reverse covariance, and invariance of the fixed initial law. Each adjacent action covers all 21,172 states, 6,556 marks, and 78,628 event atoms. Corrupting a state, mark, or event image is detected even when frame maps remain intact.

For the three-/four-large controls, the metric and whole kernel are permuted together; the same program hash and registry are retained. Thus covariance is a theorem of the generated process, not a frame-count summary.

---

## 6. Independent verifier and hostile replays

`verify_0014.py` imports no code from `generate_0014.py`. Its standard-Python implementation is stored in two adjacent canonical source shards, decoded with a pinned SHA-256, and then executed. It independently implements the DSL, expansion, state and mark construction, atom IDs, reverse pairing, ledgers, schedules, cemeteries, `S9` actions, and hostile mutations. The generator is used only as an external subprocess for byte-identical regeneration.

Baseline regeneration returned exit code 0 and byte identity. All hostile children returned 2:

| mutation | localized code |
|---|---|
| row probability | `row_sum` |
| state row deletion | `row_coverage` |
| schedule deletion | `schedule_coverage` |
| ghost mark | `dangling_mark` |
| ghost destination | `dangling_destination` |
| reverse ID | `reverse_link` |
| system energy | `energy_ledger` |
| charge quantum | `charge_ledger` |
| null physical jump | `null_projection` |
| state `S9` image | `s9_state_bijection` |
| mark `S9` image | `s9_mark_bijection` |
| event `S9` image | `s9_event_covariance` |
| cemetery removal | `cemetery_missing` |
| validity routing removal | `source_validity_routing` |
| age hold | `scheduled_clock` |
| Poisson tag | `scheduled_semantics` |
| explicit count-three | `dsl_unknown_op` |
| hidden arithmetic branch | `dsl_param_schema` |
| count alias | `dsl_unwhitelisted_leaf` |
| special-three registry | `registry_hidden_three` |
| environment/global | `dsl_unknown_op` |
| post-rank override | `dsl_unwhitelisted_leaf` |

The mutation report is computed from actual child exit statuses and parsed errors. It contains no prewritten `passed` or `expected_rejected` evidence.

---

## 7. Executed commands and canonical identities

Commands actually executed from the repository-root equivalent `/mnt/data/0014_work` were:

```bash
python3 artifacts/0014/generate_0014.py \
  --output artifacts/0014/framewise_scheduled_kernel.json \
  --verifier artifacts/0014/verify_0014.py

python3 artifacts/0014/verify_0014.py \
  --artifact artifacts/0014/framewise_scheduled_kernel.json \
  --generator artifacts/0014/generate_0014.py \
  --record /tmp/0014_baseline_record.json

python3 artifacts/0014/verify_0014.py \
  --artifact artifacts/0014/framewise_scheduled_kernel.json \
  --generator artifacts/0014/generate_0014.py \
  --crlf-only --record /tmp/0014_crlf_record.json

python3 artifacts/0014/verify_0014.py \
  --artifact artifacts/0014/framewise_scheduled_kernel.json \
  --generator artifacts/0014/generate_0014.py \
  --report artifacts/0014/hostile_mutation_report.json \
  --baseline-record /tmp/0014_baseline_record.json \
  --crlf-record /tmp/0014_crlf_record.json

python3 -m py_compile \
  artifacts/0014/generate_0014.py \
  artifacts/0014/verify_0014.py
```

All five commands exited 0. Baseline, CRLF replay, and hostile suite returned `PASS`. Canonical JSON is UTF-8 with sorted keys, comma/colon separators, and no insignificant whitespace. Script hashes normalize CRLF and lone CR to LF.

| object | SHA-256 |
|---|---|
| generator normalized LF | `ae85d6e614ced62fd6ba0dcde826a2e0629dc5a1ebdb09ef97dc91fa6ea41b87` |
| verifier normalized LF | `85a5dc8a15769b8a6abdec201eca20ecd01af531d2d8cb55b130b01ccf8772fd` |
| verifier source shard 0 | `86f5e90b3040a334c2b13a131f57523b30529de1e2596a26ee5b216c477c5b57` |
| verifier source shard 1 | `c916effff5249919cce02b4dac719153a37f6c8779a0320c368336d3d4c5df5f` |
| kernel file | `f899fe6d7ca03df843353c875421f56bb310c347f2d64ffcd4f8dc89f08b2be9` |
| kernel decoded payload | `5f0bc6aff4857282fd290f25aac63c42a9db5b64fb67c9b0fdee9abd2f31dad0` |
| mutation report file | `383815f1719fe43a47d1f42c3cacb40b565c6c716fe3c80bd65d22bbcf8be515` |
| mutation report payload | `0f31d55522d506e3669058d3a10590ea1720a2bfa726d1dc659fb1596039decf` |
| registered program | `a979ba2d545b9333ac9178a0e786763f28f119f43f3ea57c8a344f3a06403f6c` |

The verifier generated CRLF copies of both launchers plus the verifier source shards, reran canonical regeneration and independent expansion, and recovered the same normalized hashes and kernel payload.

---

## 8. Interface and theorem boundary

A later module may expand the exact countdown phases, attach the existing anonymous linear/product response stack, and define response neighborhoods before counting cardinality. Entrant strict residence and killed retention must be computed on that exact age-augmented scheduled process or by exact schedule recursion; `1/t_r` must not be silently interpreted as an exponential CTMC rate. The same kernel artifact and fixed initial law must be reused for all outputs `m=0,...,9`.

No response cell or all-rank score is exported in this round. This is deliberate: Brief 0014 closes the pre-classifier event kernel, not the dimension-selection gate.

The remaining physical gate is to derive or independently measure the sparse full-metric impact preparation, return separation/speed law, post-miss history kernel, reservoir state-counting ratio, and fixed exchangeable initial preparation. The next operational gate is to attach anonymous reconstruction to the age-augmented process and compare entrant strict residence and killed retention across all ten output ranks. Primitive string-orbit dominance, universal all-field cone dynamics, and physical retarded or entropy-current time selection remain independent.

---

## References

1. B. Greene, D. Kabat and S. Marnerides, *Dynamical Decompactification and Three Large Dimensions*, arXiv:0908.0955.
2. M. G. Jackson, N. T. Jones and J. Polchinski, *Collisions of Cosmic F- and D-strings*, arXiv:hep-th/0405229.
3. R. Danos, A. R. Frey and A. Mazumdar, *Interaction Rates in String Gas Cosmology*, arXiv:hep-th/0409162.
4. R. Easther, B. R. Greene, M. G. Jackson and D. Kabat, *String Windings in the Early Universe*, arXiv:hep-th/0409121.
5. A. R. Frey et al., near-Hagedorn/random-walk forward–reverse string interaction analysis, arXiv:2310.11494.
