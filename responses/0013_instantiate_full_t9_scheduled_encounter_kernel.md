---
brief: 0013
source: browser-hosted GPT
captured: 2026-07-23
repository_commit: 428757cf1a03fcfa57b4bc2fd316d622f1d448cb
status: proposed
artifacts:
  - artifacts/0013/reproduce_0013.py
  - artifacts/0013/kernel_ledger.json.xz.b64
  - artifacts/0013/replay_report.json
---

# Response 0013 — A populated full-T^9 scheduled encounter kernel and replayable pre-classifier ledger

## Executive result

This round closes the finite kernel-instantiation gate. It does **not** close, or even evaluate, a visible-rank-three selection gate.

The delivered object is a finite, target-neutral, event-scheduled Markov-renewal closure for one controlled opposite-winding F1 sector on a rectangular anisotropic nine-torus. It is generated before any response classifier exists and contains no microscopic rank, response cell, active mask, pole band, preferred subset, or target cardinality.

For every eligible pair, the construction keeps four objects separate:

\[
\boxed{
\text{absolute scheduled return}
\;\times\;
\text{normalized full-metric mark law}
\;\times\;
\text{conditional source channel}
\;\times\;
\text{post-event history/reset law}.
}
\tag{1}
\]

The GKM calibration is implemented with the scheduled rule

\[
t_n=n\,t_r,
\qquad
t_r=\frac{r}{\bar v},
\qquad n=1,2,\ldots,
\tag{2}
\]

and a fresh impact redraw at every scheduled return. No exponential waiting-time law is introduced. In the finite calibration,

\[
r=\frac{R_{v}}2=1,
\qquad
\bar v=\frac12,
\qquad
t_r=2
\tag{3}
\]

in the registered unit \(2\pi\sqrt{\alpha'}/c\). The first five scheduled events are therefore exactly

\[
2,4,6,8,10.
\tag{4}
\]

The populated artifact contains:

- 504 ordered anonymous local pair frames;
- 6,552 full-metric finite impact marks;
- 21,168 explicitly serialized event-epoch microscopic/history/reservoir states;
- four exactly serialized countdown phases for each event state, hence 84,672 age-augmented states by the declared Cartesian rule;
- one normalized, positive-support, rank-unconditioned \(S_9\)-exchangeable initial law;
- exact forward/reverse charge and energy resets;
- three explicit cemetery outcomes;
- the eight adjacent transpositions generating the full \(S_9\) action;
- a frozen dependency graph and train/validation registry established before any response classifier;
- outward rational intervals for the transcendental GKM shell amplitude;
- an exact interface for a later anonymous all-rank response replay, without exporting response cells.

The strongest closed result is positive about this **kernel**, not about any preferred rank.

> **Populated full-\(T^9\) scheduled-kernel theorem.**  
> The files `artifacts/0013/reproduce_0013.py`, `artifacts/0013/kernel_ledger.json.xz.b64`, and `artifacts/0013/replay_report.json` define and replay one finite pre-classifier scheduled encounter process. The process has deterministic returns \(t_n=nr/\bar v\), normalized finite marks, normalized conditional channels, explicit history-only misses, paired non-null reverse atoms, exact charge and energy accounting, source-validity cemeteries, and a nontrivial \(S_9\) action. The same full-metric constructor restricts to the registered three-large and four-large isotropic GKM controls without receiving either cardinality. Its registered dependency graph passes, while an \(S_9\)-invariant hidden-three table fails before export. Regeneration from the standard-Python source reproduces the machine-readable artifact byte for byte and reruns every listed certificate.

This is Brief 0013 outcome 1: an explicitly populated, target-neutral finite measurable closure with a faithful scheduled-GKM source limit and replayable non-encoding and ledger certificates.

It is not a theorem that three visible spatial directions are selected, preferred, stable, excluded, or even present in the later response quotient.

---

# 1. Evidence classification

## `primary-source derived`

1. Greene, Kabat and Marnerides use an impact-parameter interaction probability in a heavy, dilute winding-string regime and, in their numerical closure, estimate a recollision time by \(t_r\simeq r/\bar v\) and redraw the impact parameter at each scheduled recollision. The source is [arXiv:0908.0955](https://arxiv.org/abs/0908.0955).
2. Jackson, Jones and Polchinski give crossing-conditioned F/F, F/D, and composite-string channel probabilities with angle, velocity, coupling, and compact-wavefunction dependence. These are conditional probabilities after a crossing, not absolute encounter clocks. The source is [hep-th/0405229](https://arxiv.org/abs/hep-th/0405229).
3. Danos, Frey and Mazumdar give compact-space and time-dependent source-regime constraints. They do not supply a universal anisotropic first-return process. The source is [hep-th/0409162](https://arxiv.org/abs/hep-th/0409162).
4. Easther, Greene, Jackson and Kabat supply the anisotropic nine-torus winding–geometry setting and an explicit initial-measure caveat. Their sampled ensemble is a control, not a uniquely derived preparation. The source is [hep-th/0409121](https://arxiv.org/abs/hep-th/0409121).
5. Frey et al. derive forward/reverse near-Hagedorn rates in declared random-walk and thermodynamic regimes. This is used only as a detailed-balance registry, not as a universal reverse law. The source is [arXiv:2310.11494](https://arxiv.org/abs/2310.11494).

## `derived from a newly proposed measurable closure`

1. The full-metric ordered local frame \((w,v,s)\), consisting of a winding axis, relative-velocity axis, and scattering axis.
2. The per-normal-axis sparse impact redraw: one central mark and two signed basis marks for every normal cycle above the registered self-dual source boundary.
3. The exact rational source normalization \(p_0=1/4\) and the selected rational inside the GKM shell interval.
4. The finite reservoir degeneracies \(g_{\rm present}=1\), \(g_{\rm products}=4\), and the resulting reverse probability.
5. The positive-support exchangeable preparation on event states.
6. The finite history alphabet and persistent-history alternative control.
7. The frozen dependency registration and its train/validation split.

## `exact theorem about the declared finite model`

1. Exact deterministic schedule replay and zero waiting-time variance.
2. Exact initial-law, mark-law, and channel normalization.
3. Exact mark and channel involutions.
4. Exact charge and energy ledger closure.
5. Exact finite microcanonical detailed balance.
6. Exact \(S_9\) covariance under eight adjacent swaps.
7. Exact restriction of one constructor to three-large and four-large isotropic source controls.
8. Exact history-control distinction with the same one-mark marginal.
9. Exact clock-scale control.
10. Exact rejection of an equivariant hidden-three dependency graph.
11. Exact serialization replay and file-hash comparison.

## `controlled numerical statement`

The only transcendental factor used in the populated finite registry is \(e^{-1/4}\). It is enclosed by a rigorous alternating-series rational interval. The finite kernel selects the rational midpoint of that interval. No floating-point probability is used in a transition table.

## `open gate`

A world-sheet or compact relative-motion calculation must still derive, rather than register, the separation/return-speed distribution, the sparse impact preparation, the reservoir state counting, and the exchangeable initial preparation. Only after those inputs are derived or independently measured may the same artifact be passed to the anonymous all-rank strict-residence gate. Primitive string-orbit dominance, universal all-field cone dynamics, and physical time orientation remain separate gates.

---

# 2. Source registry and scheduled-clock correction

## 2.1 GKM scheduled branch

For a registered heavy, dilute, slow opposite-winding F1 encounter, the populated GKM table is

\[
p_{\rm ann}(b)
=p_0\exp\!\left(-\frac{b^2}{\Delta^2}\right),
\qquad
p_0=\frac14,
\qquad
\Delta^2=1.
\tag{5}
\]

The central mark has the exact conditional probability

\[
p_c=\frac14.
\tag{6}
\]

The signed shell marks have \(b^2=1/4\), so

\[
p_s=\frac14e^{-1/4}.
\tag{7}
\]

The exported outward interval is

\[
\frac{951797901453647189682343}
     {4888530788066148954931200}
< p_s <
\frac{342647244523312988285643481}
     {1759871083703813623775232000},
\tag{8}
\]

with width

\[
\frac1{1759871083703813623775232000}.
\tag{9}
\]

The exact selected transition probability is

\[
\widehat p_s=
\frac{7530708670842043698585571}
     {38678485356127771951104000},
\tag{10}
\]

which the reproducer verifies lies strictly inside (8).

The scheduled return clock is independent of (5):

\[
t_r(G,w,v,s)
=
\frac{R_v\,\xi_r}{\bar v},
\qquad
\xi_r=\frac12.
\tag{11}
\]

For the populated full-\(T^9\) cell, \(R_v=2\) and \(\bar v=1/2\), giving (3). No parameter named `rate = 1/t_r` is exported.

## 2.2 JJP calibration is separate

The exact finite JJP registry uses

\[
g_s=\frac1{10},
\qquad
V_{\min}\rho_\perp=\frac15,
\qquad
\theta=\frac\pi2,
\qquad
v=\frac35,
\tag{12}
\]

for which the registered kinematic factor is

\[
f(\theta,v)=\frac{25}{96}.
\tag{13}
\]

The resulting conditional crossing probability is

\[
p_{\rm JJP}
=\frac1{100}\frac15\frac{25}{96}
=\frac1{1920}.
\tag{14}
\]

This table is a cross-regime calibration only. It is never multiplied by the GKM table.

## 2.3 Source-invalid outcomes

The machine-readable registry has three cemetery outcomes:

```text
out_of_source_regime
unresolved_source_amplitude
energy_or_charge_failure
```

A velocity, coupling, geometry, or world-sheet state outside a registered validity cell is routed to a cemetery. A conditional probability whose interval intersects an invalid perturbative cap is unresolved. No source value is clipped into \([0,1]\) to keep the finite model running.

---

# 3. Pre-classifier microscopic state

## 3.1 Full \(T^9\) geometry

The populated metric is rectangular and anonymous:

\[
G=\operatorname{diag}(4,\ldots,4),
\qquad
R_i=2,
\qquad i=0,\ldots,8.
\tag{15}
\]

The registered self-dual source boundary is \(R_{\rm sd}=1\).

A local pair frame is an ordered triple

\[
f=(w,v,s),
\tag{16}
\]

where \(w\) is the primitive winding axis, \(v\) the relative-velocity axis, and \(s\) the local scattering axis. There are

\[
9\cdot8\cdot7=504
\tag{17}
\]

frames. Every ordered triple is present; no direction is named or omitted.

The normal impact axes are obtained by the local geometric operation

\[
N_f=\{0,\ldots,8\}\setminus\{w,v,s\}.
\tag{18}
\]

The constructor is never passed \(|N_f|\), a number of large directions, or any later response output.

## 3.2 Event-epoch physical states

For each frame, there are three present states

\[
P_h,
\qquad h\in\{\mathrm{none},\mathrm{central},\mathrm{shell}\},
\tag{19}
\]

and, for every pre-annihilation history \(h\) and every one of the 13 forward marks \(m\), one products state

\[
A_{h,m}.
\tag{20}
\]

Therefore each frame has

\[
3+3\cdot13=42
\tag{21}
\]

event states, and the full artifact explicitly serializes

\[
504\cdot42=21168
\tag{22}
\]

rows.

A present state has

```text
system energy = 2 quanta
reservoir energy = 0
net winding charge = 0
```

and a products state has

```text
system energy = 0
reservoir energy = 2 quanta
net winding charge = 0.
```

The stored forward mark in \(A_{h,m}\) makes the reverse atom non-optional and determines its time-reversed mark.

## 3.3 Exact age augmentation

The event process is semi-Markov. To make its finite schedule replayable without calling it a CTMC, the artifact supplies the exact countdown factor

\[
r\in\{4,3,2,1\},
\qquad
\delta t=\frac12.
\tag{23}
\]

For \(r>1\),

\[
(x,r)\longmapsto(x,r-1)
\tag{24}
\]

with certainty. At \(r=1\), the scheduled event kernel is applied and the countdown resets to four. The full exact age-augmented finite state is thus the Cartesian product of the 21,168 event states with the four countdown phases. The exported metadata explicitly says

```text
process_type = event-scheduled Markov renewal; not CTMC
export_as_CTMC = false
```

and a later strict-residence calculation must use this countdown chain or the equivalent exact renewal convolution.

## 3.4 Fixed exchangeable initial law

Each frame has exact weight \(1/504\). Within a frame,

\[
\Pr(P_{\rm none})=\frac14,
\qquad
\Pr(P_{\rm central})=\Pr(P_{\rm shell})=\frac18,
\tag{25}
\]

and each of the 39 products states has weight

\[
\frac1{78}.
\tag{26}
\]

Thus

\[
\frac14+\frac18+\frac18+3\cdot13\cdot\frac1{78}=1.
\tag{27}
\]

Every event state has positive mass. The law is invariant under simultaneous permutation of the nine directions and is not conditioned on a response cell or cardinality.

---

# 4. Full-metric finite marks

## 4.1 One common constructor

For a frame \(f\), the mark constructor examines every physical normal axis \(j\in N_f\). It adds signed shell marks only when the predeclared source condition

\[
R_j>R_{\rm sd}
\tag{28}
\]

holds. This is a local compactification condition registered before response reconstruction, not a pole or visibility threshold.

The finite mark set consists of:

1. one central vector \(b=0\);
2. for every source-active normal axis \(j\), the two full-vector marks
   \[
   b=\pm\frac{R_j-R_{\rm sd}}2 e_j.
   \tag{29}
   \]

In the populated full-\(T^9\) geometry, every frame has six normal axes, so it has

\[
1+2\cdot6=13
\tag{30}
\]

marks. The artifact explicitly serializes

\[
504\cdot13=6552
\tag{31}

mark records.

Each record contains the frame, physical impact axis and coordinate, \(b^2\), relative speed, angle, charges/species, source branch, world-sheet cell, source-probability key, and reverse mark ID.

## 4.2 Time reversal

The involution is

\[
\iota(0)=0,
\qquad
\iota(+e_j)=-e_j,
\qquad
\iota(-e_j)=+e_j.
\tag{32}
\]

The reproducer exhaustively verifies

\[
\iota^2(m)=m
\tag{33}
\]

for all 6,552 marks.

## 4.3 Mark normalization

The selected GKM source limit uses independent redraw. Every one of the 13 marks has exact probability

\[
\mu(m)=\frac1{13}
\tag{34}
\]

at each scheduled return. The previous mark class is retained in the state for the history control, but the populated source-limit kernel has persistence parameter \(\rho=0\).

A persistent alternative with the same one-mark marginal is generated only as a control; it is not silently substituted into the source limit.

---

# 5. Exact scheduled event and ledger semantics

## 5.1 Present-pair row

At a scheduled return from \(P_h\):

1. draw \(m\) from (34);
2. use \(p_c=1/4\) for the central mark or \(\widehat p_s\) for a shell mark;
3. with conditional probability \(p(m)\), perform the physical annihilation
   \[
   P_h\longrightarrow A_{h,m};
   \tag{35}
   \]
4. with probability \(1-p(m)\), perform a miss/history update
   \[
   P_h\longrightarrow P_{\operatorname{class}(m)}.
   \tag{36}
   \]

Equation (36) is a history-only or self event. It is not mislabeled a physical occupation jump.

Every row obeys

\[
\sum_m\mu(m)=1,
\qquad
p(m)+[1-p(m)]=1.
\tag{37}
\]

## 5.2 Products-state row and reverse channel

At a scheduled return from \(A_{h,m}\), the registered reverse mark is \(\iota(m)\). The finite reservoir weights are

\[
g_{P}=1,
\qquad
g_A=4.
\tag{38}
\]

The reverse creation probability is

\[
p_{\rm cr}(m)=\frac{g_P}{g_A}p_{\rm ann}(m)=\frac14p_{\rm ann}(m).
\tag{39}
\]

Thus

\[
A_{h,m}\longrightarrow P_h
\tag{40}
\]

with probability (39), restoring the pre-annihilation history, system energy, and time-reversed source state. With probability \(1-p_{\rm cr}(m)\), the scheduled event is a self event and the products state remains unchanged.

There is no caller-controlled boolean by which a non-null forward atom can evade its reverse record.

## 5.3 Energy and charge

For annihilation,

\[
(2_{\rm sys}+0_R)\longrightarrow(0_{\rm sys}+2_R).
\tag{41}
\]

For reverse creation,

\[
(0_{\rm sys}+2_R)\longrightarrow(2_{\rm sys}+0_R).
\tag{42}
\]

The net winding charge is zero before and after both events. The reproducer checks (41)–(42), the mark involution, and the channel involution exhaustively.

## 5.4 Null, miss, and unresolved events

The artifact distinguishes:

- scheduled return;
- selected mark;
- accepted annihilation or creation;
- rejected miss with history update;
- scheduled self event;
- source-invalid cemetery;
- unresolved-amplitude cemetery;
- energy/charge-failure cemetery.

Only a non-null accepted channel changes the physical occupation/reservoir state. A null or unresolved record never becomes an off-diagonal physical event accidentally.

---

# 6. Full-\(T^9\) covariance and non-encoding

## 6.1 Exact \(S_9\) action

For each adjacent transposition

\[
s_a=(a,a+1),
\qquad a=0,\ldots,7,
\tag{43}
\]

the artifact serializes the complete map on all 504 ordered frames. Marks transform by permuting both the frame and the physical impact axis. Status, history class, energy, and mark class are transported with the frame.

The verifier checks all eight maps against direct permutation of the frame tuples and confirms that the action is nontrivial. No \(O(9)\) covariance is claimed.

## 6.2 Frozen dependency whitelist

The registered kernel may read only:

```text
charges
species
metric_radii
dilaton_cell
warp_cell
winding_axis
velocity_axis
scattering_axis
separation_fraction
relative_speed
relative_position
relative_tangent
worldsheet_cell
history_class
stored_forward_mark
reservoir_energy
```

Allowed operations are local reads, index selection, set difference, rational arithmetic, metric length, normal projection, history mixing, source-registry lookup, reservoir ratio, and reset lookup.

Forbidden inputs include:

```text
visible_count
response_cell
active_mask
target_rank
pole_band
rank
dimension_label
```

and forbidden operations include `count_if`, `top_k`, `classifier_lookup`, and `visible_threshold`.

The complete dependency path is serialized:

\[
\text{microscopic fields}
\to
\text{schedule, normal axes, impact mark}
\to
\text{source channel and reverse ratio}
\to
\text{reset}.
\tag{44}
\]

## 6.3 Registration split

Before any response classifier is available, the registry fixes:

```text
training:
  GKM_iso3_central
  GKM_iso4_central_shell
  JJP_pi_over_2

validation:
  permuted_iso3
  permuted_iso4
  clock_separation
  clock_velocity
  persistent_history
```

No response label is used in either set.

## 6.4 Hidden-three adversary

The adversary computes

```text
global_count = count_if(metric_radii, radius > self_dual)
rate = special_value if global_count == 3 else another_value
```

This table is \(S_9\)-invariant. It nevertheless fails the dependency gate because `count_if` is unregistered and the resulting global count is not a whitelisted constitutive variable.

The executed certificate records:

```text
hidden_three_S9_invariant = true
hidden_three_accepted = false
rejection = forbidden_or_unregistered_op:count_if
```

This is stronger than a token scan and stronger than permutation covariance alone.

---

# 7. Same-law source limits

The same constructor receives the full radius vector and local ordered frame in every case. It never receives a number of large dimensions.

## Theorem 1 — source-limit restriction

Let a source-valid frame require each of its three local axes \((w,v,s)\) to satisfy \(R_i>1\). Let the normal mark rule be (28)–(29).

### Three-large isotropic stratum

For

\[
R=(2,2,2,1,1,1,1,1,1),
\tag{45}
\]

there are exactly

\[
3\cdot2\cdot1=6
\tag{46}
\]

valid ordered frames. All remaining normal radii equal the self-dual radius, so every valid frame has only the central mark:

\[
|\mathcal M_f|=1.
\tag{47}
\]

### Four-large isotropic stratum

For

\[
R=(2,2,2,2,1,1,1,1,1),
\tag{48}
\]

there are

\[
4\cdot3\cdot2=24
\tag{49}
\]

valid ordered frames. Exactly one large normal axis remains, so every valid frame has

\[
|\mathcal M_f|=1+2=3.
\tag{50}
\]

In both restrictions the scheduled time remains \(t_r=2\).

### Proof

The conclusions follow by applying the same per-axis tests to the supplied full metric. No count is formed or passed to the constructor. The reproducer enumerates every frame and verifies (46)–(50).

This is the finite scheduled-GKM source-limit theorem required by the brief. It is not an all-rank selection theorem.

---

# 8. Controls separating clock, marks, channels, and history

## 8.1 Scheduled-clock control

The exact replay gives

```text
unique hold times: 2
first five events: 2, 4, 6, 8, 10
waiting-time variance: 0
poisson_clock: false
```

Replacing the schedule by an exponential clock would fail the certificate.

## 8.2 Clock-scale control

With the central mark held fixed,

\[
(r,\bar v)=(1,1/2)
\quad\Rightarrow\quad t_r=2,
\tag{51}
\]

\[
(r,\bar v)=(3/2,1/2)
\quad\Rightarrow\quad t_r=3,
\tag{52}
\]

and

\[
(r,\bar v)=(1,1/4)
\quad\Rightarrow\quad t_r=4.
\tag{53}
\]

In all three cases the fixed-mark conditional channel remains \(1/4\). Thus the absolute clock is not hidden inside the channel probability.

## 8.3 History control

Let

\[
p_c=\frac14,
\qquad
p_s=\widehat p_s.
\tag{54}
\]

For four scheduled encounters, iid redraw gives survival

\[
S_{\rm iid}(4)
=
\left[
\frac1{13}(1-p_c)
+
\frac{12}{13}(1-p_s)
\right]^4,
\tag{55}
\]

whereas a fully persistent mark class with the same one-mark marginal gives

\[
S_{\rm pers}(4)
=
\frac1{13}(1-p_c)^4
+
\frac{12}{13}(1-p_s)^4.
\tag{56}
\]

The executed exact calculation reports

```text
S_iid  = 0.411746357838...
S_pers = 0.412550327392...
difference = 0.000803969554...
```

The one-mark marginal is identical. The finite-horizon survival changes because the recollision history changes.

## 8.4 Six-state occupancy control

Response 0011 remains a separate average-occupancy adversary. It is not part of this kernel and is not cited as strict-residence evidence.

---

# 9. Interface to the later all-rank gate

The machine-readable artifact exports only pre-classifier objects:

```text
event states
fixed initial law
scheduled holds
age/countdown augmentation
full-metric marks
conditional channels
history updates
forward/reverse ledger
S9 action
source/cemetery provenance
```

It explicitly exports

```text
exports_response_cells = false
```

A later module must attach the same complete all-direction Green/product panel to trajectories generated from this unchanged artifact. It may then reconstruct anonymous local response words and only afterwards define cells \(Z_0,\ldots,Z_9\).

Because this process is nonexponential, entrant strict residence must be evaluated on the exact countdown-augmented process or by exact renewal convolution. It is forbidden to feed the unaugmented event kernel into CTMC killed-semigroup formulas.

No response cell is manufactured in this round. Consequently no rank-three margin—positive or negative—is reported.

---

# 10. Machine-readable artifact

`artifacts/0013/kernel_ledger.json.xz.b64` serializes:

1. every 504 ordered frame;
2. every 6,552 finite mark and its reverse ID;
3. every 21,168 event-epoch state;
4. the exact countdown factor and deterministic update rule;
5. the normalized fixed initial law;
6. all scheduled times and physical units;
7. all present-pair mark/channel/reset rows;
8. all products-state reverse rows;
9. system and reservoir energy quanta and net charge;
10. all three cemetery outcomes and routing rules;
11. the eight adjacent \(S_9\) frame maps;
12. the two isotropic source-limit tables;
13. the complete dependency graph and hidden-three adversary;
14. source-registry entries and approximation/unresolved-boundary ledger;
15. the later all-rank consumer contract.

The payload is canonical UTF-8 JSON with sorted keys and exact rational strings. For repository transport it is deterministically XZ-compressed with a fixed preset and ASCII-armored as base64; the verifier decodes it and checks exact canonical regeneration. No symbolic placeholder named merely `T`, `K`, `R`, `Omega`, or `mu` stands in for an unpopulated table.

---

# 11. Deterministic reproducer and executed commands

The reproducer uses only the Python standard library:

```text
argparse
hashlib
itertools
json
fractions
pathlib
```

No random number generator and no third-party package is used.

The exact commands actually executed were:

```bash
python3 artifacts/0013/reproduce_0013.py \
  --write artifacts/0013/kernel_ledger.json.xz.b64 \
  --report artifacts/0013/replay_report.json

python3 artifacts/0013/reproduce_0013.py \
  --verify artifacts/0013/kernel_ledger.json.xz.b64 \
  --verify-report artifacts/0013/replay_report.json
```

In the execution environment these commands completed with exit status zero. The verify command regenerated the complete model, reloaded the JSON, reran all certificates, and compared artifact and script hashes with the recorded report.

## 11.1 Executed hashes

```text
artifacts/0013/reproduce_0013.py
sha256 = 9c9af73a314261e173709f9123871026640b6a3095475d0e0e2cfbd4f00e6432

artifacts/0013/kernel_ledger.json.xz.b64
sha256 = 834215c9f241c4f1a3d796e5a012855fbe7156714dad55286649c107baf68cd5
canonical-payload sha256 = 62a5d667fa0aa31e15bfdd64760457e3cb89ea033c5470b50e98b0825d137722
ASCII-armored deterministic-XZ bytes = 46017

artifacts/0013/replay_report.json
sha256 = 94b15982a970996128eb5a8538eb2764029df24d2d00199d755c4e1eb2b6a663
```

## 11.2 Tests actually executed

The report contains 15 executed tests, all with `passed: true`:

```text
frame_count
state_count
mark_count
initial_law_normalization
conditional_normalization
JJP_exact_registry
reverse_microcanonical_detailed_balance
ledger_charge_reverse_involution
scheduled_clock_replay
full_T9_covariance
same_law_isotropic_source_limits
history_control
clock_control
nonencoding_dependency_gate
outward_source_interval
```

No unexecuted test is described as passed.

---

# 12. Mandatory replay ledger

| required control | executed result |
|---|---|
| conditional-law normalization | exact sums equal one |
| forward/reverse ledger | exact energy and charge closure |
| reverse involution | all 6,552 marks satisfy \(\iota^2=1\); channel involution passes |
| scheduled clock | events at 2, 4, 6, 8, 10; zero waiting-time variance; non-Poisson |
| full-\(T^9\) covariance | all eight adjacent generators pass; action nontrivial |
| same-law three-large limit | 6 valid frames, one mark each |
| same-law four-large limit | 24 valid frames, three marks each |
| history control | same marginal, strict survival changes by about \(8.04\times10^{-4}\) |
| clock control | delays change from 2 to 3 or 4; fixed-mark channel stays \(1/4\) |
| hidden-three adversary | equivariant graph rejected before export |
| serialization replay | artifact and report hashes match regenerated values |

---

# 13. Strongest closed theorem

> **Full-\(T^9\) scheduled encounter-kernel theorem.**  
> Fix the populated metric, ordered local-frame rule, scheduled separation and velocity, finite full-metric mark preparation, GKM and JJP source registries, reverse reservoir ratio, event and history resets, exchangeable initial law, source-validity cemeteries, dependency registry, and exact countdown augmentation serialized in `artifacts/0013/kernel_ledger.json.xz.b64`.
>
> Then:
>
> 1. the resulting pre-classifier process is finite and mathematically complete relative to the declared sparse mark closure;
> 2. its encounter schedule is \(t_n=nr/\bar v\), with no exponential-clock substitution;
> 3. scheduled encounters, accepted reactions, misses, history-only updates, self events, and cemetery exits are distinct records;
> 4. every non-null forward atom has a serialized reverse atom and all noncemetery physical events conserve the declared energy and charge;
> 5. the process is exactly \(S_9\)-covariant but makes no \(O(9)\) claim;
> 6. the same full-metric constructor reduces to the registered three-large and four-large scheduled-GKM controls without receiving either count;
> 7. the registered pre-classifier dependency graph is accepted, while an \(S_9\)-invariant hidden-three table is rejected;
> 8. deterministic standard-Python regeneration reproduces the canonical artifact and all 15 executed certificates.
>
> Hence Brief 0013 outcome 1 is achieved: an explicitly populated, target-neutral finite measurable closure with a faithful scheduled-GKM source limit and replayable non-encoding and ledger certificates.

The theorem is exact for the finite kernel and its declared sparse impact law. The approximation ledger explicitly leaves continuum impact support, dynamical geometry drift, and microscopic preparation derivation unresolved.

No visible-rank-three selection or exclusion is claimed.

The next hard gate is to derive or independently measure the full-metric scheduled separation/return-speed ensemble, impact preparation beyond the finite registered marks, reverse reservoir density law, and exchangeable initial preparation; then consume this same artifact in one anonymous all-rank entrant-strict-residence calculation. Universal all-field cone dynamics and physical retarded or entropy-current time selection remain separate unfinished gates.

---

# References

1. Brian Greene, Daniel Kabat and Stefanos Marnerides, [*Dynamical Decompactification and Three Large Dimensions*](https://arxiv.org/abs/0908.0955).
2. Mark G. Jackson, Nicholas T. Jones and Joseph Polchinski, [*Collisions of Cosmic F- and D-strings*](https://arxiv.org/abs/hep-th/0405229).
3. Rebecca Danos, Andrew R. Frey and Anupam Mazumdar, [*Interaction Rates in String Gas Cosmology*](https://arxiv.org/abs/hep-th/0409162).
4. Richard Easther, Brian R. Greene, Mark G. Jackson and Daniel Kabat, [*String Windings in the Early Universe*](https://arxiv.org/abs/hep-th/0409121).
5. Andrew R. Frey et al., [*String Thermodynamics In and Out of Equilibrium: Boltzmann Equations and Random Walks*](https://arxiv.org/abs/2310.11494).
