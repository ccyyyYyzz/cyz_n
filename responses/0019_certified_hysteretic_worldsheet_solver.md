---
brief: 0019
source: local certified implementation with browser-hosted GPT co-theory
captured: 2026-07-23
status: locally-verified
---

# Response 0019 — exact closed-string no-entry fixture and the remaining first-entry gate

## Result

The first preregistered valid Brief 0018 source now has an authoritative
physical finite-window outcome.  After correcting the circumference
serialization, an exact symbolic-\(\pi\) closed-string problem, its source
measure bridge, an outward-rounded Arb cover and source-separated replay give

\[
\boxed{\texttt{source index 2: right\_censored\_no\_entry}} .
\]

This is a result on the true closed world-sheet quotient.  It is not the old
cut-open rectangular control, an all-time statement, a source-population
probability, or a \(3+1\) selection result.

Brief 0019 is therefore closed for the finite-window no-entry branch of this
one fixed source.  The general hysteretic first-entry solver remains open for
population states that cannot be excluded by this branch.

## 1. The serialization defect and its consequence

The frozen Brief 0018 registry stored

\[
\widehat L=16\,\operatorname{binary64}(\pi)
=\frac{884279719003555}{2^{44}},
\qquad \widehat k_1=\frac18 .
\]

The proof backend evaluates mathematical Arb sine and cosine.  Hence
\(\widehat k_1\widehat L-2\pi\ne0\), and the transverse endpoint jumps are
certifiably nonzero.  The first 179-node certificate is mathematically valid
only on a cut-open numerical strip.  It cannot be promoted to a closed-string
quotient by identifying its endpoints.

That certificate remains useful as a negative serialization control.  It is
not the adopted physical outcome.

## 2. Versioned exact geometric realization

The corrected physical realization changes no Brief 0018 source hash.  It
receives a new geometric problem hash and declares

\[
L_*=16\pi,\qquad T_F=\frac1{2\pi},\qquad
M=T_FL_*=8,\qquad k_n=\frac n8 .
\]

Set

\[
u_i=\frac{\sigma_i}{L_*}\in\mathbb R/\mathbb Z,\qquad
\xi_i^w=\frac{X_i^w}{L_*}.
\]

In these coordinates,

\[
\Lambda=\operatorname{diag}(8,\ldots,8,1),\qquad
G=\operatorname{diag}(1,\ldots,1,(16\pi)^2),
\]

and the physical metric on the solver chart \((u_1,u_2,t)\) is

\[
H=\operatorname{diag}((16\pi)^2,(16\pi)^2,1).
\]

Coordinate root isolation uses the chart Jacobian.  Encounter singular values
must use \(G^{1/2}JH^{-1/2}\).

The transverse phases are \(2\pi n u_i\), so they are exactly periodic.
For orientations \((+1,-1)\), both \(u_1\mapsto u_1+1\) and
\(u_2\mapsto u_2+1\) shift the separation winding coordinate and its image
index by \(+1\); the corner shift is \(+2\).  The upper-to-lower chart rule is
\(n_{\rm lower}=n_{\rm upper}-a_i e_w\).  These are integer lattice
reindexings, not approximate endpoint equalities.

The canonical hashes are

```text
lift registry   c80acb64eeeb3133dff4422fc798f5b75c6feb52cf32502888cac452e2d210a1
closed problem  3bb6599f211c26d98ecba2077051ad9d0339daf96d580a6399cc5a1ba7f030e0
lift fixture    8ab0bf7deca71d4a1d11f7656a2ee45dbe0766e4ca8ce886592559b080d24f9e
```

An independent replayer rebuilds all three from the separately replayed
source state.  Its only project dependency is the source-binding replayer;
it imports neither the lift generator, Arb jets nor a solver.

## 3. Why the source law is preserved

The lift is not justified merely by saying that the coefficient bytes were
kept.  The source law was checked at both finite-sequence and analytic levels.

The old binary64 generator already gives exactly

\[
\widehat M=8,\qquad \widehat k_1=\frac18,\qquad
\widehat E_*=2-\frac{|P|^2}{4\widehat M}=1.
\]

These equal the new exact values.  Thus the registered radial law remains
\(\operatorname{Dirichlet}(4,15,15)\), the chiral linear Jacobian factor is
unchanged, the relative-centre Haar periods remain eight, and the random
streams, orientations, total momentum and seed remain identical.

The latent-variable and coefficient maps are identities with Jacobian one.
Consequently the new-over-old Radon--Nikodym derivative of the analytic
Brief 0018 unconditioned source law is exactly one.

All 512 registered source states were regenerated.  Their state sequence,
binary64 coefficient payloads and exact \(q\pi^0\) transports are identical,
with no validity conditioning or redraw.  For source index 2, all 109 source
floating leaves round-trip exactly and every one of the 98 coefficient atoms
carried by the physical problem is compared directly.

This does not imply that the old and new physical embeddings have the same
event pushforward.  The event law must be recomputed in the corrected
geometry.

## 4. Quotient-safe no-entry proof

On each box \(B\subset[0,1)^2\times[t_0,t_1)\), the Arb backend evaluates the
closed hull and encloses each transverse separation coordinate \(d_A(B)\).
For transverse period eight and \(r_{\rm out}=1/2\), define

\[
n_{\min}=
\left\lceil\frac{\inf d_A(B)-1/2}{8}\right\rceil,\qquad
n_{\max}=
\left\lfloor\frac{\sup d_A(B)+1/2}{8}\right\rfloor .
\]

If \(n_{\min}>n_{\max}\), with both adjacent exact-dyadic margins strictly
positive, no \(8\mathbb Z\) image of that coordinate lies within \(1/2\).
Then no full target image lies within \(1/2\), regardless of the winding
coordinate.  This implication is valid because \(G\) is block diagonal and
its transverse block is \(I_8\).

Touching remains unresolved.  Every proof is evaluated on the closed hull,
which is stronger than the owned half-open partition.  World-sheet seam
coverage is exact because the transverse functions are periodic and the
winding image is not used.

The deterministic cover has

```text
nodes              259
split nodes        129
excluded leaves    130
maximum depth        9
unresolved leaves    0
axis counts          [14,66,12,10,4,0,0,24]
```

Its commitments are

```text
solver registry  23e404021dcae9b4c75dca810feb404afe6786aa14280f0ae88b0fa4f24fcec5
root node        78d1589b4049483b2865e434ef3b3227a576daee654be042c9cd63419474c636
certificate      d2cd11d1f8fd1b3669d988f590ee619e9a7f5ee6af43b3a0671830abc69f7fe1
solver report    a29941afbecca913f9b7bee6812b2590f8db733b7993053b7bbe05b1cd5a1817
replayer report  48a2a888501cb084af4c3fc2e3277482e59e19c62057253eddb3d399fbebbcd4
```

The solver reserves nodes for unbuilt right siblings.  Every legal odd node
budget therefore ends in a complete binary partition with typed unresolved
leaves rather than an internal failure.

## 5. Evidence classes

The adopted statement rests on five distinct layers:

1. independent Brief 0018 source reconstruction;
2. exact symbolic-\(\pi\) source and geometry transport;
3. independent lift reconstruction;
4. generator-side fail-closed Arb cover; and
5. an independent no-entry replay that reconstructs the transverse equations
   and recomputes every leaf enclosure without importing the solver or jet
   implementation.

Strict JSON rejects floats, nonfinite numbers, duplicate keys and Boolean
integers.  All symbolic \(q\pi^e\) and dyadic encodings are canonical.
Hostile fixtures reseal ordinary hashes after changing coefficients, source
commitments, metrics, lattice periods, seams, boxes, splits, Arb enclosures,
witness axes, radius, outcome or scope.  Rejection must occur at the
corresponding semantic gate.

## 6. What is and is not closed

The following are closed:

- the exact source-bound symbolic closed-string realization for index 2;
- preservation of the registered unconditioned Brief 0018 source law under
  that lift;
- exact world-sheet seam and target-image algebra;
- the finite-window no-entry branch for source index 2; and
- deterministic budget-safe certificate generation and independent replay.

The following remain open:

- population-wide problem construction and replay;
- exhaustive roots for states that approach or cross \(r_{\rm out}\) or
  \(r_{\rm in}\);
- global spatial minimality, earliest-time ordering and complete image
  competition;
- seam ties, singular roots, grazing and positive-dimensional root sets;
- hysteretic outer exit, re-arm and completed-episode closest approach;
- the unconditioned joint law of
  \((T,j,b,\ell,a,\sigma_3)\);
- \(K\), energy, period and time-horizon sensitivity;
- anonymous response-rank residence dynamics; and
- a stable visible \(3+1\) basin or a model-level exclusion theorem.

## 7. Next gate

Regenerate all 512 registered sources.  Route every source-invalid state
before any geometry backend.  Bind each valid state to its own symbolic
closed-string problem and first apply the quotient-safe transverse no-entry
fast path.  Preserve every unresolved box and send only those boxes to the
image-complete first-entry solver.

The population ledger must remain total and disjoint:

\[
\text{source-invalid}
\;\dot\cup\;
\text{finite-window no-entry}
\;\dot\cup\;
\text{certified event classes}
\;\dot\cup\;
\text{typed unresolved}.
\]

Only after this pushforward closes may the project estimate event masses,
rank mixtures and event-conditioned singular-value tails.  Those outputs,
not the fixed source index and not the local F1 ceiling \(a\le3\), are the
input to dimension-selection dynamics.
