---
brief: 0010
source: browser-hosted GPT
captured: 2026-07-23
repository_commit: 780f0f1b540f9b4aa1b958fee53113e02071883f
status: proposed
---

# Response 0010 — A complete exchangeable finite hybrid, the arbitrary-rank transition theorem, and the all-rank selection no-go

## Executive result

The source-authorized winding–geometry construction can be repaired into one complete finite operator and one valid arbitrary-cardinality transition statement. It still does not derive a preference for three.

The repaired model has:

- no microscopic dimension variable;
- no visible-rank variable;
- no event projector;
- no preferred subset of directions;
- no rate branch keyed to three or to a target cardinality;
- one S₉-exchangeable initial law;
- one S₉-equivariant finite generator;
- one pre-quotient port and product-response system;
- one predeclared bandwidth and error model for every output rank.

The finite state contains nine anonymous directional geometry and occupation sectors, a shifted-dilaton branch, a finite energy reservoir, and a finite probe/Galerkin sector. Winding and momentum reactions alter only occupations and the reservoir. Geometry moves only through a finite-volume approximation to the anisotropic dilaton-gravity drift. Every source jump leaves the geometry indices unchanged.

The central finite operator is

```text
𝑸θ = 𝑸hyb,θ + 𝑨probe,θ + 𝑴reset,θ.                         (1)
```

Its invariant probability channel is the conservative Markov generator Qθ. First passage, residence, leakage, response poles, Laurent residues, linear transfer and mixed/product responses are all derived from 𝑸θ or from killed restrictions of that same operator.

The corrected conditional result is valid for every

```text
m ∈ {0,1,…,8}.
```

If a response-defined visible-(m+1) predecessor has m cleared, noncontracting directions and one wound marginal direction, then the marginal direction can recollapse continuously and leave the response band. The proof uses the correct two-stage comparison inequality: one estimate before the velocity reaches zero and the opposite damping endpoint after it becomes negative. The attraction bound protects all 9−m wound directions over one full horizon, includes the clearance time, separates deterministic response enclosures from probability errors, and treats every tube-exit face through a killed generator or an explicit full-horizon bound.

The genuine all-rank question is posed with one exchangeable initial law μθ and response-defined cells Zm. For one observation window, define

```text
Im(θ) = μθ(Zm),                                                (2)

Hm(T;θ) = Pμθ[τZm ≤ T],                                       (3)

Rm(T0,T1;θ)
= Eμθ[(T1−T0)⁻¹ ∫[T0,T1] 1Zm(Xt) dt].                         (4)
```

Let Lm(T;θ) be the worst-case finite-horizon exit probability from Zm. Initial mass Im and dynamical attraction Hm−Im are reported separately.

The sharp result is negative.

> **Exchangeable all-rank underdetermination theorem.**
>
> Fix the primary-source isotropic restrictions, the anisotropic dilaton-gravity equations, one exchangeable initial-law class, and all pre-quotient response conventions. Suppose the anisotropic collision and recollision kernel is specified only on the source isotropic strata, as in the current source-authorized family. Then the ordering of the ten residence scores Rm is not determined. On every source-unconstrained anisotropic transition orbit, an arbitrarily small positive S₉-equivariant rate perturbation can be chosen to vanish on all source restrictions while changing the residence scores. Under a mild reachability condition, for every k in {0,…,9} there is a source-compatible completion for which Zk is the unique residence winner.
>
> Consequently no open-cell uniform margin
>
> ```text
> R3 ≥ maxm≠3 Rm + δ
> ```
>
> follows from the present source/closure data. Exchangeability removes named axes; it does not select a cardinality.

This is stronger than observing that the previous theorem works after replacing 3 by m. It proves that, over the actual source-compatible completion fiber, the winner can be changed without changing any currently authorized source restriction.

The equal-hazard negative control makes the same point in closed form. If the nine anonymous directional visibility indicators evolve independently with common birth and death rates, then

```text
Kt ~ Binomial(9, pt),
```

and every rank k from zero through nine is the unique mode on an open rate-ratio cell. A three-winning cell exists mathematically, but so does an equivalent cell for every other rank. Unless the source action fixes the rate ratio, this is not an explanation of three.

The smallest missing non-target-keyed object is finite and measurable. For the finite executable it is the complete vector

```text
Korb(T)
= { μθ(O), ae(θ) :
    O is a reachable S₉-state orbit,
    e is a reachable anisotropic edge orbit }.                 (5)
```

In the continuum description this is the normalized normal-bundle recollision/impact kernel. A convenient rank-neutral Gaussian form is

```text
γ̄ab(X)
= νab(X) Cab(X)
  det′[I + 2 Σab⊥(X) / Δab²(X)]^(−1/2).                        (6)
```

Here Σab⊥ is the measured impact covariance on the normal bundle of the two histories and det′ is taken on its support. Formula (6) contains no rank and no target cardinality. It can be measured from marked recollisions. Without its anisotropic dependence, or an action deriving it, the all-rank residence ordering is unidentifiable.

Thus the required conclusion is option 2 of Brief 0010:

> a complete arbitrary-m conditional model plus a sharp no-go showing that the current source/closure family does not select three, followed by the smallest measurable non-target-keyed missing interaction object.

---

# 1. Classification of claims

Every substantive statement belongs to exactly one required class.

## `primary-source derived`

1. The anisotropic nine-torus shifted-dilaton equations and directional pressure coupling of Tseytlin–Vafa and Easther–Greene–Jackson–Kabat.
2. Winding and momentum energies and the signs of their directional pressures.
3. The Greene–Kabat–Marnerides fixed-dimension impact-dependent winding-annihilation amplitude in its heavy, dilute, slow regime.
4. The fixed-d Boltzmann structure and the explicitly conditional Hagedorn-fluctuation scenario.
5. The rule that a source-authorized winding event changes winding occupations, not geometry or operational rank.

## `derived from a newly proposed action`

1. The finite anisotropic recollision and impact kernel.
2. The energy-quantized occupation-level reaction lift.
3. The democratic zero/first-harmonic probe action.
4. The quadratic modulation port used to recover multiplication and mixed response data.
5. The finite-volume geometry generator and its residual ledger.

## `exact theorem about a declared model`

1. The finite-generator validity theorem.
2. The one-operator first-passage, residence, leakage and resolvent formulas.
3. The corrected arbitrary-m recollapse theorem.
4. The full-horizon occupation bounds.
5. The equal-hazard binomial control.
6. The source-compatible all-rank underdetermination theorem.
7. The minimal orbit-rate data theorem.

## `controlled asymptotic/numerical conjecture`

1. Convergence of the upwind finite-volume process to the continuous dilaton-gravity PDMP.
2. Use of the GKM eikonal amplitude over a finite anisotropic impact ensemble.
3. Passive-probe and finite Fourier/Galerkin convergence.
4. Emergence of an exchangeable source-valid initial law from a realistic Hagedorn fluctuation measure.

## `no-go/underdetermination`

1. Exchangeability alone does not select a cardinality.
2. Isotropic source restrictions do not determine the anisotropic residence ordering.
3. A factorized equal-hazard model can make any output rank the mode.
4. An all-or-nothing isotropic control does not prefer three.
5. The conditional m+1-to-m theorem does not imply an all-rank winner.
6. The present executable does not select a primitive stringlike charge orbit, one time direction or a universal all-field cone.

## `open gate`

A duality-complete action must derive the anisotropic normal-impact kernel and the exchangeable initial fluctuation law, prove a primitive stringlike rank-active orbit gap, derive a universal all-field carrier, and select a physical retarded or entropy-current time component.

---

# 2. One target-neutral finite state space

## 2.1 Geometry grid and exact constraint branch

Choose finite rational grids

```text
Rgrid = {R0,…,RJ} ⊂ positive rationals,
Hgrid = {h−M,…,hM},
Phigrid = {φ̄0,…,φ̄L}.                                        (7)
```

The radius grid is closed under inversion. Choose one energy quantum ε0 such that

```text
qW(R) = 2R / ε0 is an integer,
qK(R) = 2R⁻¹ / ε0 is an integer                              (8)
```

for every R in Rgrid.

The finite directional state is

```text
ξi = (ai,bi,wi+,wi−,ki+,ki−),                                 (9)
```

with

```text
ai ∈ {0,…,J},
bi ∈ {−M,…,M},
wi±,ki± ∈ {0,…,Nmax}.                                         (10)
```

The common variables are

```text
c ∈ {0,…,L},
ℓ ∈ {0,…,Lmax},
ρ ∈ Rnum.                                                      (11)
```

The quantity ℓ ε0 is pressureless reservoir energy. The finite variable ρ is a numerical work-residual ledger and is not a physical source field.

For a state x, define

```text
E(x)
= ε0 ℓ
  + Σi [Rai(wi+ + wi−) + Rai⁻¹(ki+ + ki−)],                   (12)

Pi(x)
= −Rai(wi+ + wi−) + Rai⁻¹(ki+ + ki−).                         (13)
```

The negative shifted-dilaton branch is fixed by the exact algebraic constraint

```text
ν(x)
= −sqrt[Σi hbi² + exp(φ̄c) E(x)/(2π)²].                        (14)
```

The finite source-valid set Sθ contains only states satisfying the predeclared energy, weak-coupling, heavy-string, dilute-gas, grid and coefficient bounds. Add one cemetery state †.

There is no state field called rank, dimension, large-count, selected subset or projector.

## 2.2 Relative topology

The exact continuum state space is a disjoint union over occupation vectors. It carries the coproduct topology: occupations have the discrete topology and each fixed-occupation Hamiltonian-constraint branch has its relative Euclidean topology. A positive-radius conditional tube means open in this relative topology.

The finite parameter space is a product of compact coefficient boxes and probability simplices. An open parameter cell means open in the relative interior of that product, with fixed cutoff combinatorics and strictly positive support weights.

## 2.3 One exchangeable initial law

For every physical parameter θ, declare one law

```text
μθ(x)
= Zθ⁻¹ 1Sθ(x) ρθ(c,ℓ,ρ) Πi ηθ(ξi | c,ℓ).                      (15)
```

All directional factors are identical and the source-valid indicator is S₉-invariant. Therefore

```text
μθ(σx) = μθ(x) for every σ in S₉.                             (16)
```

Neither ρθ nor ηθ may condition on a response rank, a number of clear directions or a preferred subset. Initial mass in every response cell is measured after the law has been declared.

---

# 3. The complete finite probability generator

## 3.1 Upwind geometry drift

Use reduced continuous coordinates

```text
y = (λ1,…,λ9,h1,…,h9,φ̄).
```

At fixed occupations and reservoir, the source vector field is

```text
Fλi = hi,                                                      (17)

Fhi = ν hi + exp(φ̄) Pi /(8π²),                               (18)

Fφ̄ = ν.                                                       (19)
```

Between source reactions,

```text
dE0/dt = 0.                                                    (20)
```

For each grid coordinate r, let xr+ and xr− be adjacent grid states and let Δr± be the corresponding spacings. Define

```text
qdg(x,xr+) = max(Fr(x),0)/Δr+,
qdg(x,xr−) = max(−Fr(x),0)/Δr−.                               (21)
```

If an indicated neighbor is absent or outside the source-valid domain, send the same rate to †. A geometry transition changes no occupation and no physical reservoir variable.

For a geometry edge x→y, define midpoint work

```text
Wxy = −Σi [Pi(x)+Pi(y)] [λi(y)−λi(x)] / 2.                    (22)
```

Update the numerical residual ledger by

```text
ρ(y) = ρ(x) + Wxy − [E(y)−E(x)].                              (23)
```

If the updated residual leaves its predeclared interval, send the transition to †. Every accepted finite-volume edge satisfies

```text
E(y)+ρ(y) = E(x)+ρ(x)+Wxy.                                    (24)
```

## 3.2 Exactly one impact semantics

All impact marks are integrated out. No persistent mark state and no separate mark generator are present.

For each eligible oppositely wound pair in direction i, choose a finite impact grid Bi, a normalized probability vector

```text
Σb∈Bi μi(b|x) = 1,                                             (25)
```

and an absolute recollision frequency νi(x). The thinned accepted pair hazard is

```text
γWi−(x)
= νi(x) Σb∈Bi μi(b|x) min{1, PGKM(b|x)}.                       (26)
```

The GKM factor is primary-source derived on its declared source strata. The generic anisotropic μi and νi are new measurable closures.

## 3.3 Winding reactions

The winding-annihilation intensity is

```text
aWi−(x) = γWi−(x) wi+ wi−.                                    (27)
```

Its reset is

```text
(wi+,wi−,ℓ)
→ (wi+−1,wi−−1,ℓ+qW(Rai)).                                   (28)
```

The reverse channel has an independently declared coefficient γWi+(x) and propensity

```text
aWi+(x)
= γWi+(x) (ℓ)qW / qW!,                                        (29)
```

with reset

```text
(wi+,wi−,ℓ)
→ (wi++1,wi−+1,ℓ−qW(Rai)).                                   (30)
```

Here (n)r is the falling factorial. In the radiation control γWi+ is zero. In a Hagedorn control it must be present and independently calibrated.

## 3.4 Momentum reactions

The momentum pair channels are

```text
aKi−(x) = γKi−(x) ki+ ki−,                                    (31)

(ki+,ki−,ℓ)
→ (ki+−1,ki−−1,ℓ+qK(Rai)),                                   (32)

aKi+(x) = γKi+(x) (ℓ)qK / qK!,                                (33)

(ki+,ki−,ℓ)
→ (ki++1,ki−+1,ℓ−qK(Rai)).                                   (34)
```

The momentum coefficients are explicit closure parameters unless a primary source fixes them in the chosen thermodynamic regime.

## 3.5 Cutoff boundary rules

A reaction rate is zero when a reactant or reservoir quantum is absent. If a physically enabled reaction would create an occupation above Nmax, a reservoir level above Lmax, or a state outside the declared source regime, its full unsuppressed intensity is sent to †. It is not silently reflected or deleted.

The signed charges

```text
qWi = wi+−wi−,
qKi = ki+−ki−                                                   (35)
```

are preserved by all pair channels. At fixed geometry every non-cemetery reaction conserves the exact quantized matter energy (12).

## 3.6 Matrix definition

Let qocc(x,y) be the sum of all occupation-reaction intensities from x to y, including cemetery transitions. For x≠y, define

```text
Qθ(x,y) = qdg(x,y) + qocc(x,y).                               (36)
```

Set

```text
Qθ(x,x) = −Σy≠x Qθ(x,y),
Qθ(†,†) = 0.                                                   (37)
```

Equations (7)–(37) fully specify the finite Markov generator. There is no undefined drift, mark process, reaction or cutoff rule.

## Theorem 1 — finite-generator validity

Assume all finite coefficients are nonnegative and all directional parameter arrays are permuted covariantly. Then:

1. every off-diagonal entry of Qθ is nonnegative;
2. every row sums to zero;
3. all non-cemetery reaction jumps conserve directional charges and exact quantized matter energy;
4. the numerical work identity (24) holds on every geometry edge;
5. the finite process is nonexplosive;
6. † is absorbing and records every source, cutoff or residual exit;
7. Qθ(σx,σy)=Qθ(x,y) for every σ in S₉;
8. no O(9) covariance is implied.

### Proof

Items 1–2 follow from (36)–(37). Falling-factorial propensities vanish when reactants are absent. Equations (28)–(34) transfer exactly the quantized pair energy to or from the reservoir and preserve signed charges. The state space is finite, so total exit rate is bounded and explosion is impossible. Simultaneous permutations commute with the source vector field, impact closure, reactions, cutoffs and ports. A general continuous rotation does not preserve the diagonal torus coordinate family.

## 3.7 Finite-volume enclosure

For a twice differentiable continuum test function f, upwind Taylor expansion gives

```text
||Qdg ιh f − ιh(F·grad f)||∞
≤ ½ Σr Δr,max ||Fr||∞ ||∂rr f||∞ + εcell(f).                   (38)
```

The term εcell encloses coefficient variation inside cells. The total deterministic approximation budget is

```text
εmod = εFV + εimpact + εGal.                                   (39)
```

No response margin may be claimed unless it exceeds εmod after semigroup or resolvent propagation.

---

# 4. One augmented operator for probability and response

## 4.1 Probe and product ports

Let Vpr be a finite Fourier/two-jet space containing, for every anonymous direction,

```text
1, cos θi, sin θi,                                              (40)
```

all products required through declared degree two, and every declared probe polarization. At finite geometry state x, let Ax be the Galerkin matrix of the democratic minimally coupled probe action.

Add one quadratic modulation port

```text
Smult
= −½ Σα ∫dt vα(t) ∫d⁹θ sqrt(g9) χα(θ) ψᵀKψ.                   (41)
```

The χα span the required products of zero and first harmonics. Mixed source/modulation/readout derivatives therefore provide the multiplication and character data. The response quotient is not inferred from a linear transfer matrix alone.

## 4.2 The augmented matrix

Set

```text
V = span{eprob} ⊕ Vpr,
Ax eprob = 0.
```

Every finite Markov transition carries probe coefficients by a declared reset map Ryx satisfying Ryx eprob=eprob. On the finite space of state-indexed probe vectors, define

```text
(𝑸θ Z)x
= Ax Zx
  + Σy≠x Qθ(y,x) Ryx Zy
  − [Σy≠x Qθ(x,y)] Zx.                                        (42)
```

The probability subspace spanned by state vectors tensored with eprob is invariant and carries exactly Qθ. Probability and response calculations are therefore restrictions of one finite operator, not unrelated surrogates.

## 4.3 Linear and mixed transfers

For source vectors Ba, readouts Cb and modulation operators Mα, define

```text
Gba(z) = Cb (zI−𝑸θ)⁻¹ Ba,                                     (43)

G²b;αa(z2,z1)
= Cb (z2I−𝑸θ)⁻¹ Mα (z1I−𝑸θ)⁻¹ Ba.                            (44)
```

All poles and Laurent coefficients come from 𝑸θ. For a simple eigenvalue ζj with right and left vectors rj and ℓj,

```text
Resj = (C rj)(ℓj* B)/(ℓj* rj).                                (45)
```

A zero residue is an unobserved mode. A repeated eigenvalue uses the complete Laurent principal part.

## 4.4 Local response cells are killed restrictions

For a finite response-observation cell C, let 𝑸θ,C be the restriction of 𝑸θ with every exit sent to a cemetery channel. The local response experiment uses

```text
C (zI−𝑸θ,C)⁻¹ B.                                               (46)
```

This is derived from the same full operator. The probability of leaving the cell during the response window is computed from the corresponding killed probability block and enters the response-validity margin.

## 4.5 Response-defined rank cells

Fix a bandwidth interval I=[Λ−,Λ+] and every spectral, residue, observability, relation and approximation margin before declaring any output. Run the complete anonymous relation/Green reconstruction on (43)–(46).

Define

```text
Zm(θ,I)
= {x : the complete local response certificate returns
       visible spatial cotangent rank m}.                     (47)
```

The sets Zm are introduced only after the generator, initial law, ports, band and errors are fixed. They are unions of S₉-orbits. No state transition consults them.

The local Green matrices are inverted and the principal biform is obtained through the adopted algebraic bridge

```text
Hx∨(pL,x) = βx,
Lx = μ2,x∨(ker Hx∨).                                           (48)
```

Ordered inertia is an observed property. It is not passed to the mechanism. The declared minimally coupled probes provide an executable common-cone control, not a universal all-field theorem.

## 4.6 Exact all-rank probability formulas

Let Pm be the diagonal projector onto Zm. Let Qnotm and Qm be the killed subgenerators on the complement and on Zm. With row-vector convention,

```text
Im = μθ Pm 1,                                                  (49)

Hm(T) = 1 − μθ Pnotm exp(T Qnotm) 1,                           (50)

Am(T) = Hm(T) − Im,                                            (51)

Rm(T0,T1)
= (T1−T0)⁻¹ μθ ∫[T0,T1] exp(tQθ) Pm 1 dt,                     (52)

Lm(T)
= 1 − minx∈Zm exᵀ exp(TQm) 1.                                  (53)
```

These expressions include every exit face: occupation, pressure, gap, source-validity, residue, observability, approximation and cemetery exits.

A robust three-selection margin would require an open relative parameter cell Θ* with

```text
infθ∈Θ* [R3(θ) − maxm≠3 Rm(θ) − εR(θ)] > 0,                   (54)
```

where εR is a deterministic semigroup enclosure or an explicitly labeled statistical coverage allowance. An unlabeled norm error is never added to a probability.

---

# 5. Corrected arbitrary-m conditional theorem

This section concerns the source continuous geometry inside a forward tube. It proves a transition lemma, not an all-rank preference.

## 5.1 Correct wound-direction comparison

Assume on a forward interval

```text
−uBar ≤ ν(t) ≤ −uUnder < 0,
c(t) = exp(φ̄(t))/(8π²) ≥ cUnder > 0,                           (55)
```

and suppose a marginal direction obeys

```text
Pj(t) ≤ −Pminus < 0,
hj(0) ≤ Hplus.                                                  (56)
```

Put a=cUnder Pminus.

## Lemma 1 — two-stage recollapse bound

If hj(0)≥0, define

```text
t0 = uUnder⁻¹ log[1 + uUnder hj(0)/a].                         (57)
```

Then hj reaches zero no later than t0.

After the first zero, define

```text
η = a/(2uBar),
t1 = t0 + log(2)/uBar.                                         (58)
```

Then

```text
hj(t) ≤ −η for t≥t1                                            (59)
```

as long as (55)–(56) hold. The region hj≤−η is forward invariant.

The maximal positive expansion before the first zero is bounded by

```text
Iplus
= hj(0)/uUnder
  − a uUnder⁻² log[1 + uUnder hj(0)/a].                        (60)
```

For a trap radius λtr below λj(0), a valid recollapse time is

```text
Trec
= t1 + max[λj(0)+Iplus−λtr,0]/η.                               (61)
```

If hj(0)<0, the first-zero stage has length zero and the negative-side comparison starts immediately.

### Proof

While hj≥0,

```text
dhj/dt ≤ −uUnder hj − a.
```

The comparison solution reaches zero at (57). Once hj≤0, the correct upper endpoint is uBar:

```text
dhj/dt ≤ −uBar hj − a.
```

Starting from zero, this comparison reaches −a/(2uBar) after log(2)/uBar. At hj=−η the derivative is at most −a/2, so upward crossing is impossible. Integrating the positive comparison gives (60); integrating hj≤−η gives (61).

## 5.2 Cleared directions

Assume

```text
Pi(t) ≥ Pplus > 0,
hi(0) ≥ 0.                                                      (62)
```

Put aplus=cUnder Pplus. While hi≥0,

```text
dhi/dt ≥ −uBar hi + aplus.                                     (63)
```

At hi=0 the lower bound is positive, so the nonnegative region is forward invariant, and

```text
hi(t)
≥ hi(0) exp(−uBar t)
  + (aplus/uBar)[1−exp(−uBar t)] > 0.                          (64)
```

## 5.3 Full-horizon occupation bounds

Fix m in {0,…,8}. Let L be an unlabeled set of m cleared low-gap directions, let j* outside L be one wound marginal low-gap direction, and let the remaining directions be wound high-gap directions.

Set

```text
TA = max(Trec,Tclr) + Tobs.                                    (65)
```

For every trapped direction j outside L, let qj be the number of annihilations required to leave its winding-pressure buffer and define

```text
aBarj = supx∈U aW,j−(x).                                       (66)
```

Then

```text
εtrap(TA)
= Σj∉L P[Pois(aBarj TA) ≥ qj]                                  (67)
```

protects all 9−m wound directions over the whole attraction horizon. Losses are not reset window by window.

For each clear direction i in L, suppose in the radiation control that reverse creation vanishes and

```text
aW,i−(x) ≥ γUnderi ni whenever ni>0.                           (68)
```

Then

```text
εclr(Tclr)
= Σi∈L ni(0) exp(−γUnderi Tclr).                               (69)
```

In a regime with reverse creation, either use the exact killed matrix Qθ or add the full-horizon compensator bound

```text
εcr(TA) = 1 − exp(−cBarA TA),                                  (70)
```

where cBarA is a uniform upper bound for total reverse-creation intensity. No death intensity is assumed to remain bounded by its initial value.

Let

```text
εdagger(TA) = 1 − exp(−qBarDagger TA)                           (71)
```

bound finite-grid, source-validity and residual-ledger cemetery exits. Response and approximation conditions are deterministic interval margins. If they are statistical, their coverage probability is a separately named αcov.

## Theorem 2 — conditional visible-(m+1)-to-visible-m transition

For any m in {0,…,8}, assume:

1. the source forward tube and deterministic response margins remain valid;
2. the m clear directions satisfy (62)–(64);
3. the marginal direction satisfies Lemma 1;
4. all 9−m wound directions satisfy the buffer bound (67);
5. the m clear directions satisfy (69), with reverse creation treated by (70) or by the exact killed generator;
6. the local response cell survives for the observation interval and the complete relation/Green certificate is valid before and after crossing;
7. the finite-model error is smaller than every spectral and relation margin.

Then a response-defined visible-(m+1) predecessor reaches a response-defined visible-m cell by time TA with probability at least

```text
1 − εtrap − εclr − εcr − εdagger − αcov.                       (72)
```

The exact total tube-exit probability after arrival is (53). If only occupation faces are estimated analytically, the result is explicitly called an occupation-exit bound rather than a total tube-exit bound.

### Proof

The full-horizon Poisson domination protects the marginal and every other wound direction until the geometry comparison is complete. Lemma 1 sends the marginal first-harmonic gap through the predeclared band. The cleared-direction bound preserves the m low-gap directions. Clearance and reverse-creation estimates control their occupation faces. Every other exit is either ruled out by a deterministic enclosure or counted by the cemetery/killed generator. The response cell therefore changes from m+1 to m on the complement of the listed failure events.

### Mandatory counterfactuals

The same theorem and code must succeed for at least

```text
m=2, m=3, m=4.
```

Failure only away from m=3 is evidence of a hidden target branch.

---

# 6. The all-rank comparison from one law and one generator

## 6.1 Score vector

For fixed θ, one initial law (15), one operator (42), one band and one error model produce

```text
Sθ = {(Im, Am(T), Rm(T0,T1), Lm(T)) : m=0,…,9}.                (73)
```

The output rank is not used to choose a simulation, initial condition, port, bandwidth or transition law.

A genuine three-selection certificate requires:

1. A3(T)>0, so the result is not merely initial mass;
2. positive basin mass outside Z3;
3. the robust margin (54);
4. a finite leakage bound L3(T)≤ε*;
5. robustness to the predeclared band and model-error box;
6. the same finite operator reproducing first-passage and response data.

No such source-derived margin is presently available.

## 6.2 Equal-hazard control

Consider the exact subfamily in which the nine directional visibility indicators are independent, identically distributed two-state chains:

```text
0 --α--> 1,
1 --β--> 0.                                                    (74)
```

Let the initial law be product Bernoulli with parameter p0. Then

```text
p(t)
= α/(α+β)
  + [p0−α/(α+β)] exp[−(α+β)t],                                (75)

Kt ~ Binomial(9,p(t)),                                         (76)

Rk(T0,T1)
= (T1−T0)⁻¹ ∫[T0,T1]
  choose(9,k) p(t)^k [1−p(t)]^(9−k) dt.                        (77)
```

At stationarity p*=α/(α+β), and rank k is the unique mode exactly when

```text
k < 10 p* < k+1.                                               (78)
```

Every k from zero through nine is therefore the unique winner on an open rate-ratio cell. Exchangeability and identical local rules do not privilege three. If a three advantage comes from p0 and disappears after burn-in, it is initial-measure bias.

## 6.3 Easther–Greene–Jackson–Kabat all-or-nothing control

On the diagonal subspace

```text
λi=λ, hi=h, wi±=w±, ki±=k± for every i,                        (79)
```

with identical directional marks, S₉ symmetry keeps all directions equal. All first-harmonic gaps cross a fixed response band simultaneously. Intermediate cells Z1,…,Z8 are not visited on this invariant control. It is an all-or-nothing decompactification control, not a three preference.

## 6.4 Non-three and tie cells

The finite executable must include:

- a physical rate cell whose residence winner is two;
- a physical rate cell whose residence winner is four;
- an equal-hazard cell with a tie or no separated winner;
- a band perturbation for which the former winner loses its margin.

These controls prevent plotting or labeling conventions from being mistaken for selection.

---

# 7. The sharp all-rank underdetermination theorem

## 7.1 Source-compatible completion fiber

Let Csrc be the finite family of pairs (μ,Q) satisfying:

1. the same primary-source isotropic hazard restrictions;
2. the same source geometry equations and reaction authorization;
3. the same cutoffs, ports, band and errors;
4. S₉-exchangeability of μ;
5. S₉-equivariance of Q;
6. positivity and row-sum zero;
7. no geometry jump at a source reaction.

The anisotropic recollision rates away from the source strata are not fixed by those conditions.

## 7.2 Residence derivative

For a finite generator Q, define

```text
Mk(Q)
= (T1−T0)⁻¹ ∫[T0,T1] exp(tQ) Pk 1 dt,
Rk = μ Mk(Q).                                                  (80)
```

For any row-sum-zero perturbation ΔQ, Duhamel differentiation gives

```text
D Rk(Q)[ΔQ]
= (T1−T0)⁻¹ ∫[T0,T1] ∫[0,t]
  μ exp(sQ) ΔQ exp((t−s)Q) Pk 1 ds dt.                         (81)
```

If this pairing is nonzero, the sign of a sufficiently small perturbation can increase or decrease Rk.

## Theorem 3 — source-compatible residence underdetermination

Assume that for each nonempty rank cell Zk there is at least one reachable S₉-edge orbit outside the source isotropic strata such that the pairing (81) is nonzero. Then:

1. for every k there is an arbitrarily small, positivity-preserving, S₉-equivariant perturbation of anisotropic rates that leaves every primary-source restriction unchanged but changes Rk;
2. the ordering of (R0,…,R9) is not identified by the source restrictions;
3. no uniform positive three-margin can hold over the complete source-compatible fiber;
4. if unconstrained edge orbits permit entry-rate enhancement and exit-rate suppression on every Zk, then for every k there is a completion for which Zk is the unique residence winner.

### Proof

Parameterize every positive free rate by

```text
qe(ε) = qe(0) exp(ε σe),                                       (82)
```

where σe is constant on S₉-edge orbits and zero on source-constrained edges. Off-diagonal positivity is automatic and the diagonal is reset to preserve row sums. Equation (81) proves the local statement. For the global statement, lower all exit rates from a reachable S₉-orbit contained in Zk and increase its unconstrained entry rates. In the finite chain the mean residence in that orbit tends to one as the entry/exit ratio diverges, while all source restrictions remain fixed.

The perturbations in this proof are not proposed physical laws. They are indistinguishable source-compatible completions. Their existence proves that current source data do not determine a winner.

## 7.3 Initial-law underdetermination

If the exchangeable initial law is not independently measured, another ambiguity remains. For any bounded S₉-invariant function gk supported in Zk, define

```text
με(x)
= μ0(x) exp[ε gk(x)]
  / Σy μ0(y) exp[ε gk(y)].                                     (83)
```

This preserves exchangeability and source-valid support while changing both Ik and Rk. A residence advantage must therefore survive burn-in and must not be attributed only to initial mass.

## Corollary 3.1 — no source-derived three-selection margin

Within the present source/closure family,

```text
inf(μ,Q)∈Csrc [R3 − maxm≠3 Rm] ≤ 0.                            (84)
```

The correct verdict is `inconclusive` about a preference for three. This does not exclude a specific completed microscopic action from selecting three; it shows that the presently authorized family does not identify such an action or margin.

---

# 8. The smallest measurable non-target-keyed missing term

## 8.1 Finite orbit-rate form

For a finite horizon T, let OT be the reachable S₉-state orbits and let ET/S₉ be the reachable anisotropic edge orbits. The minimal finite object fixing the all-rank score vector is

```text
Korb(T)
= { μ(O), ae : O∈OT and e∈ET/S₉ }.                             (85)
```

If one component with nonzero derivative (81) is left free, the residence ordering remains underdetermined. This is minimal relative to the chosen finite abstraction.

## 8.2 Continuum normal-impact term

For a pair of string histories a,b, let Pab⊥ be the anonymous projector onto their geometrically defined normal impact space. It is computed from their tangents and velocities, not from a response rank. Let

```text
Σab⊥(X) = Pab⊥ Σab(X) Pab⊥.                                    (86)
```

For a Gaussian impact preparation, the averaged source factor is

```text
hab(X)
= det′[I + 2 Σab⊥(X)/Δab²(X)]^(−1/2).                          (87)
```

The absolute accepted hazard is

```text
γ̄ab = νab Cab hab.                                             (88)
```

Every quantity in (86)–(88) is measurable from recollision times, impact marks, geometry and accepted-event counts. No dimension label or target cardinality appears.

A microscopic action deriving Σab⊥ would close the generator ambiguity. Once (85), or its continuum equivalent, is fixed, formulas (49)–(54) either certify an all-rank three margin or exclude it for that completed model.

## Theorem 4 — minimality of the anisotropic kernel

Data restricted to source isotropic strata cannot determine Korb(T). Any smaller object omitting a reachable anisotropic edge orbit with nonzero derivative (81) cannot determine the residence ordering. Therefore the normal-impact/recollision kernel together with exchangeable initial orbit weights is the smallest measurable non-target-keyed completion of the present finite model.

---

# 9. Joint forward compatibility and response falsifiability

All attraction and leakage predictions are computed from Qθ by (50) and (53). Every linear and mixed response is computed from 𝑸θ by (43)–(44). A killed restriction is used only for a predeclared hold or exit event and is explicitly derived from the full matrix.

The exact nonstationary response is

```text
Gba(t2,t1) = Cb exp[(t2−t1)𝑸θ] Ba.                             (89)
```

A quasi-stationary reduction may be used only if a resolvent or semigroup error is enclosed. For

```text
𝑸θ = 𝑨 + ε 𝑺
```

and a spectral point at distance d from spec(𝑨), the resolvent identity gives, when ε||𝑺||<d,

```text
||(zI−𝑸θ)⁻¹ − (zI−𝑨)⁻¹||
≤ ε||𝑺|| / [d(d−ε||𝑺||)].                                     (90)
```

The model is jointly falsified if no one θ reproduces:

- absolute accepted and reverse event counts;
- geometry-bin transition statistics;
- all-rank hitting and residence scores;
- finite-horizon leakage;
- observable poles and Laurent residues;
- mixed/product response data;
- response-band crossings.

The word `identifiable` is used only if the complete parameter-to-data map is proved injective modulo registered gauge. Otherwise the result is a joint compatibility and falsifiability test.

---

# 10. Mandatory all-rank adversaries

## 10.1 Hidden target branch

Insert a rate, initial-law weight, geometry potential or cutoff that changes when a count equals three.

**Required result:** static non-encoding audit rejects the model.

## 10.2 Direct geometry or rank reset

Allow a winding event to change a radius, velocity, response relation or projector instantaneously.

**Required result:** source authorization and generator audit fail.

## 10.3 Conditional-lemma cross-check

Run exactly the same code and parameter conventions for m=2,3,4.

**Required result:** each conditional theorem works when its physical tube hypotheses hold. Failure only away from three reveals target coding.

## 10.4 Equal-hazard control

Use (74) with exchangeable product initial data.

**Required result:** report the binomial score vector (77). Any three advantage caused by p0 is initial-measure bias.

## 10.5 All-or-nothing control

Use the diagonal invariant sector (79).

**Required result:** no intermediate-rank preference is reported.

## 10.6 Non-three winners and ties

Use physical rate cells producing rank-two, rank-four and tied winners.

**Required result:** report the actual winner or no separated winner.

## 10.7 Band/error perturbation

Enlarge the predeclared band or approximation box until a residue, relation or gap margin closes.

**Required result:** the winner changes or the answer becomes `inconclusive`. No tolerance fills an unproved interval.

## 10.8 Quotient-only observability

Delete one pre-quotient port direction while keeping low-band response complete.

**Required result:** microscopic non-hiding is unresolved. The claimed observable sector is stated exactly; no full-state Hautus claim is made.

## 10.9 Different probability and response operators

Fit residence with one matrix and transfer poles with another.

**Required result:** joint-operator audit rejects the model.

## 10.10 Zero-residue mode

Include an eigenvalue annihilated by source or readout.

**Required result:** it is not counted as an observed response direction or departure.

## 10.11 Retarded boundary smuggling

Use a retarded probe Green function and call its support a dynamically selected time arrow.

**Required result:** reject the time-selection claim. The source cosmology supplies its time branch in this executable.

---

# 11. Closed-loop verdict semantics

Let MD be the raw finite microscopic candidate fiber defined only by data consistency, source validity and predeclared complexity bounds. A three-winning residence score is not a membership condition.

For every candidate M, distinguish

```text
Tall(M)
= the actual completed model has a robust all-rank three margin, (91)

Rall(M,D)
= the registered finite route proves that margin and all bounds. (92)
```

If MD is empty, report model-class/data incompatibility separately. For a nonempty fiber:

1. `certified within class` requires Tall and Rall for every candidate, complete response/coverage artifacts and one full microscopic/response orbit;
2. `class-excluded` requires an actual or positively separated obstruction for every candidate;
3. every other case is `inconclusive`.

Theorem 3 obstructs certification over the present incomplete source/closure fiber because it contains completions with different residence winners. It does not exclude future completed actions from selecting three.

---

# 12. Finite executable

Use one finite parameter box with:

1. nine radius and velocity grids;
2. winding and momentum occupations in N0 up to Nmax;
3. a quantized reservoir and numerical residual ledger;
4. the integrated thinned impact kernel (26);
5. all four pair-reaction channels (27)–(34);
6. one cemetery state for every cutoff and source exit;
7. two minimally coupled probe species;
8. zero, first and degree-two harmonic ports;
9. one predeclared bandwidth family;
10. one S₉-exchangeable initial law (15).

The implementation must:

1. construct every entry of Qθ and 𝑸θ;
2. verify off-diagonal positivity, row sums and S₉ covariance exactly;
3. compute every Zm only after response replay;
4. compute (49)–(53) for all m from zero through nine;
5. report initial mass and attraction separately;
6. run the arbitrary-m theorem for m=2,3,4;
7. run equal-hazard and all-or-nothing controls;
8. optimize the robust margin (54) over the complete parameter/error box;
9. evaluate every pole and residue of the same augmented matrix;
10. replay every adversary in Section 10.

The finite theorem is exact for the declared matrix and interval enclosures. Convergence to the continuous cosmology remains a controlled numerical conjecture governed by (38)–(39).

---

# 13. Evidence ledger

| classification | result |
|---|---|
| `primary-source derived` | Anisotropic shifted-dilaton drift; winding/momentum energy and pressure signs; fixed-d GKM impact amplitude and source reaction authorization; conditional Hagedorn scenario. |
| `derived from a newly proposed action` | Integrated anisotropic impact closure; exact reservoir reaction lift; finite-volume geometry operator; democratic probe and quadratic modulation ports. |
| `exact theorem about a declared model` | Complete finite generator; conservation and symmetry theorem; one-operator score and response formulas; corrected arbitrary-m theorem; equal-hazard binomial control; source-compatible all-rank underdetermination; minimal orbit-rate data theorem. |
| `controlled asymptotic/numerical conjecture` | Finite-volume convergence; finite anisotropic use of the GKM amplitude; probe/Galerkin convergence; source derivation of the exchangeable initial law. |
| `no-go/underdetermination` | Exchangeability does not select cardinality; isotropic source restrictions leave residence ordering free; equivalent open winner cells exist for all ranks in the equal-hazard control; the conditional lemma does not select three. |
| `open gate` | Derive or measure the complete normal-impact kernel and initial orbit weights; derive primitive string-orbit dominance, a universal all-field carrier and a physical time orientation. |

---

# 14. Minimal theorem and required conclusion

> **Exchangeable all-rank selection-gate theorem.**
>
> Let the finite winding–geometry model be defined by (7)–(42), with one exchangeable initial law, one equivariant finite generator, one democratic pre-quotient linear/mixed port system and one predeclared bandwidth/error family. Let Zm be introduced only after anonymous reconstruction.
>
> Then all rank hitting, attraction, residence and leakage quantities are given exactly by (49)–(53), and all response poles and residues are derived from the same augmented operator. For every m from zero through eight, the physical tube hypotheses of Theorem 2 imply a valid conditional visible-(m+1)-to-visible-m transition with the corrected geometry and full-horizon probability bounds.
>
> However, if anisotropic recollision/impact rates and exchangeable initial orbit weights are constrained only by currently available isotropic source restrictions, Theorem 3 applies. The residence ordering is underdetermined, and no uniform source-derived three-selection margin follows. Exchangeability removes preferred axes but does not prefer the cardinality three.
>
> The smallest finite missing object is the reachable orbit-rate and initial orbit-weight vector (85), whose continuum counterpart is the measurable normal-impact covariance kernel (86)–(88). Once that target-neutral object is derived or measured, the all-rank margin (54) becomes a finite theorem or a finite exclusion test for the completed model.

This is conclusion 2 required by Brief 0010:

> **The project now has a complete arbitrary-m conditional model and a sharp no-go for three-selection in the current source/closure family.**

The remaining hard gate is to derive from a duality-complete microscopic action one exchangeable initial fluctuation law and the full anisotropic normal-impact kernel, and then prove that their all-rank residence vector has a robust three-margin. Closing the complete 3+1 loop still additionally requires a primitive stringlike orbit gap, a universal all-field carrier and a physical retarded or entropy-current time component.

---

# References

1. A. A. Tseytlin and C. Vafa, *Elements of String Cosmology*, arXiv:hep-th/9109048.
2. Richard Easther, Brian R. Greene, Mark G. Jackson and Daniel Kabat, *String Windings in the Early Universe*, arXiv:hep-th/0409121.
3. Brian Greene, Daniel Kabat and Stefanos Marnerides, *Dynamical Decompactification and Three Large Dimensions*, arXiv:0908.0955.
4. Brian Greene, Daniel Kabat and Stefanos Marnerides, *On Three Dimensions as the Preferred Dimensionality of Space via the Brandenberger–Vafa Mechanism*, arXiv:1212.2115.
5. Mairi Sakellariadou, *Numerical Experiments in String Cosmology*, arXiv:hep-th/9511075.
