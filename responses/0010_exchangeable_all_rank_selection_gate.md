---
brief: 0010
source: browser-hosted GPT
captured: 2026-07-23
repository_commit: 780f0f1b540f9b4aa1b958fee53113e02071883f
status: proposed
---

# Response 0010 — A complete exchangeable finite hybrid, the arbitrary-rank transition theorem, and the all-rank selection no-go

## Executive result

The source-authorized winding–geometry construction can be repaired into one
complete finite operator and one valid arbitrary-cardinality transition
statement.  It still does **not** derive a preference for three.

The repaired model has:

- no microscopic dimension variable;
- no visible-rank variable;
- no event projector;
- no preferred subset of directions;
- no rate branch keyed to three or to a target cardinality;
- one \(S_9\)-exchangeable initial law;
- one \(S_9\)-equivariant finite generator;
- one pre-quotient port and product-response system;
- one predeclared bandwidth and error model for every output rank.

The finite state contains nine anonymous directional geometry and occupation
sectors, a shifted-dilaton branch, a finite energy reservoir, and a finite
probe/Galerkin sector.  Winding and momentum reactions alter only occupations
and the reservoir.  Geometry moves only through a finite-volume approximation
to the anisotropic dilaton-gravity drift.  Every source jump leaves the geometry
indices unchanged.

The central finite operator is

\[
\boxed{
\mathbb Q_\theta
=
\mathbb Q_{\rm hyb,\theta}
+
\mathbb A_{\rm probe,\theta}
+
\mathbb M_{\rm reset,\theta}.
}
\tag{1}
\]

Its invariant probability channel is the conservative Markov generator
\(Q_\theta\).  First passage, residence, leakage, response poles, Laurent
residues, linear transfer and mixed/product responses are all derived from
\(\mathbb Q_\theta\) or from killed restrictions of that same operator.

The corrected conditional result is valid for every

\[
m\in\{0,1,\ldots,8\}.
\]

If a response-defined visible-\((m+1)\) predecessor has \(m\) cleared,
noncontracting directions and one wound marginal direction, then the marginal
direction can recollapse continuously and leave the response band.  The proof
uses the correct two-stage comparison inequality: one estimate before the
velocity reaches zero and the opposite damping endpoint after it becomes
negative.  The attraction bound protects all \(9-m\) wound directions over one
full horizon, includes the clearance time, separates deterministic response
enclosures from probability errors, and treats every tube-exit face through a
killed generator or an explicit full-horizon bound.

The genuine all-rank question is then posed with one exchangeable initial law
\(\mu_\theta\) and response-defined cells \(Z_m\).  For one observation window,
set

\[
I_m(\theta)=\mu_\theta(Z_m),
\tag{2}
\]

\[
H_m(T;\theta)
=
\Pr_{\mu_\theta}(\tau_{Z_m}\le T),
\tag{3}
\]

\[
R_m(T_0,T_1;\theta)
=
\mathbb E_{\mu_\theta}
\left[
\frac1{T_1-T_0}
\int_{T_0}^{T_1}\mathbf1_{Z_m}(X_t)\,dt
\right],
\tag{4}
\]

and let \(L_m(T;\theta)\) be the worst-case finite-horizon exit probability
from \(Z_m\).  Initial mass \(I_m\) and dynamical attraction
\(H_m-I_m\) are reported separately.

The sharp result is negative.

> **Exchangeable all-rank underdetermination theorem.**  
> Fix the primary-source isotropic restrictions, the anisotropic
> dilaton-gravity equations, one exchangeable initial-law class, and all
> pre-quotient response conventions.  Suppose the anisotropic collision and
> recollision kernel is specified only on the source isotropic strata, as in the
> current source-authorized family.  Then the ordering of the nine residence
> scores \(R_m\) is not determined.  On every source-unconstrained anisotropic
> transition orbit, an arbitrarily small positive, \(S_9\)-equivariant rate
> perturbation can be chosen to vanish on all source restrictions while changing
> the residence scores.  Under a mild reachability condition, for every
> \(k\in\{0,\ldots,9\}\) there is a source-compatible completion for which
> \(Z_k\) is the unique residence winner.
>
> Consequently no open-cell uniform margin
>
> \[
> R_3\ge \max_{m\ne3}R_m+\delta
> \]
>
> follows from the present source/closure data.  Exchangeability removes named
> axes; it does not select a cardinality.

This is stronger than observing that the previous theorem works after
\(3\mapsto m\).  It proves that, over the actual source-compatible completion
fiber, the winner can be changed without changing any currently authorized
source restriction.

The equal-hazard negative control makes the same point in closed form.  If the
nine anonymous directional visibility indicators evolve independently with
common birth and death rates, then

\[
K_t\sim{\rm Binomial}(9,p_t),
\]

and every rank \(k=0,\ldots,9\) is the unique mode on an open rate-ratio cell.
A three-winning cell exists mathematically, but so does an equivalent cell for
every other rank.  Unless the source action fixes the rate ratio, this is not an
explanation of three.

The smallest missing non-target-keyed object is finite and measurable.  For the
finite executable it is the complete vector of:

\[
\boxed{
\mathcal K_{\rm orb}^{(T)}
=
\left(
\mu_\theta(\mathcal O),
\ a_e(\theta)
\right)_{
\substack{
\mathcal O\;\text{reachable }S_9\text{-state orbit},\\
 e\;\text{reachable anisotropic edge orbit}
}}
}
\tag{5}
\]

through the declared observation horizon.  In the continuum description this
is the normalized normal-bundle recollision/impact kernel.  A convenient
rank-neutral Gaussian form is

\[
\bar\gamma_{ab}(X)
=
\nu_{ab}(X)C_{ab}(X)
\det{}'
\left(
I+rac{2\Sigma^{\perp}_{ab}(X)}{\Delta_{ab}^{2}(X)}
\right)^{-1/2},
\tag{6}
\]

where \(\Sigma^{\perp}_{ab}\) is the measured impact covariance on the normal
bundle of the two histories and \(\det{}'\) is taken on its support.  Formula
(6) contains no rank and no target cardinality.  It can be measured from marked
recollisions.  Without its anisotropic dependence, or an action deriving it,
the all-rank residence ordering is unidentifiable.

Thus the required conclusion is option 2 of Brief 0010:

> a complete arbitrary-\(m\) conditional model plus a sharp no-go showing that
> the current source/closure family does not select three, followed by the
> smallest measurable non-target-keyed missing interaction object.

---

# 1. Classification of claims

Every substantive statement below is assigned exactly one required label.

## `primary-source derived`

1. The anisotropic nine-torus shifted-dilaton equations and directional pressure
   coupling of Tseytlin–Vafa and Easther–Greene–Jackson–Kabat.
2. Winding and momentum energies and the signs of their directional pressures.
3. The Greene–Kabat–Marnerides fixed-dimension impact-dependent winding
   annihilation amplitude in its heavy, dilute, slow regime.
4. The fixed-\(d\) Boltzmann structure and the explicitly conditional
   Hagedorn-fluctuation scenario.
5. The rule that a source-authorized winding event changes winding occupations,
   not geometry or operational rank.

## `derived from a newly proposed action`

1. The finite anisotropic recollision and impact kernel.
2. The energy-quantized occupation-level reaction lift.
3. The democratic zero/first-harmonic probe action.
4. The quadratic modulation port used to recover multiplication and mixed
   response data.
5. The finite-volume geometry generator and its residual ledger.

## `exact theorem about a declared model`

1. The finite generator, row-sum, nonexplosion, charge and energy bookkeeping
   theorem.
2. The one-operator first-passage, residence, leakage and resolvent formulas.
3. The corrected arbitrary-\(m\) recollapse theorem.
4. The full-horizon occupation bounds.
5. The equal-hazard binomial control.
6. The source-compatible all-rank underdetermination theorem.
7. The minimal orbit-rate data theorem.

## `controlled asymptotic/numerical conjecture`

1. Convergence of the upwind finite-volume process to the continuous
   dilaton-gravity PDMP.
2. Use of the GKM eikonal amplitude over a finite anisotropic impact ensemble.
3. Passive-probe and finite Fourier/Galerkin convergence.
4. Emergence of an exchangeable source-valid initial law from a realistic
   Hagedorn fluctuation measure.

## `no-go/underdetermination`

1. Exchangeability alone does not select a cardinality.
2. Isotropic source restrictions do not determine the anisotropic residence
   ordering.
3. A factorized equal-hazard model can make any output rank the mode.
4. An all-or-nothing isotropic control does not prefer three.
5. The conditional \(m+1\to m\) theorem does not imply an all-rank winner.
6. The present executable does not select a primitive stringlike charge orbit,
   one time direction or a universal all-field cone.

## `open gate`

A duality-complete action must derive the anisotropic normal-impact kernel and
the exchangeable initial fluctuation law, prove a primitive stringlike
rank-active orbit gap, derive a universal all-field carrier, and select a
physical retarded or entropy-current time component.

---

# 2. One target-neutral finite state space

## 2.1 Geometry grid and exact constraint branch

Choose finite rational radius and velocity grids

\[
\mathcal R
=
\{R_0,\ldots,R_J\}
\subset\mathbb Q_{>0},
\qquad
\mathcal H
=
\{h_{-M},\ldots,h_M\},
\tag{7}
\]

and a finite shifted-dilaton grid

\[
\mathcal P
=
\{\bar\phi_0,\ldots,\bar\phi_L\}.
\tag{8}
\]

The radius grid is chosen so that it is closed under inversion and, for one
energy quantum \(arepsilon_0>0\),

\[
q_W(R)=\frac{2R}{\varepsilon_0}\in\mathbb N,
\qquad
q_K(R)=\frac{2R^{-1}}{\varepsilon_0}\in\mathbb N
\tag{9}
\]

for every \(R\in\mathcal R\).

The finite directional state is

\[
\xi_i
=
(a_i,b_i,w_i^+,w_i^-,k_i^+,k_i^-),
\tag{10}
\]

where

\[
a_i\in\{0,\ldots,J\},
\quad
b_i\in\{-M,\ldots,M\},
\quad
w_i^\pm,k_i^\pm\in\{0,\ldots,N_{\max}\}.
\tag{11}
\]

The common variables are

\[
c\in\{0,\ldots,L\},
\qquad
\ell\in\{0,\ldots,L_{\max}\},
\qquad
\rho\in\mathcal R_{\rm num},
\tag{12}
\]

where \(\ell\varepsilon_0\) is the pressureless reservoir energy and \(ho\)
is a finite numerical work-residual ledger.  It is not a physical source
field.

For a finite state \(x\), set

\[
E(x)
=
\varepsilon_0\ell
+
\sum_{i=1}^{9}
\left[
R_{a_i}(w_i^++w_i^-)
+
R_{a_i}^{-1}(k_i^++k_i^-)
\right],
\tag{13}
\]

\[
P_i(x)
=
-R_{a_i}(w_i^++w_i^-)
+
R_{a_i}^{-1}(k_i^++k_i^-),
\tag{14}
\]

and define the negative shifted-dilaton branch by the exact algebraic
constraint

\[
\nu(x)
=
-\sqrt{
\sum_{i=1}^{9}h_{b_i}^{2}
+
\frac{e^{\bar\phi_c}}{(2\pi)^2}E(x)
}.
\tag{15}
\]

The finite source-valid set \(\mathsf S_\theta\) contains only states satisfying
the predeclared energy, weak-coupling, heavy-string, dilute-gas, grid and
coefficient bounds.  Add one cemetery state \(\dagger\).

There is no state field called rank, dimension, large-count, selected subset or
projector.

## 2.2 Relative topology

The exact continuum state space is a disjoint union over occupation vectors.
It carries the coproduct topology: occupations have the discrete topology and
each fixed-occupation Hamiltonian-constraint branch has its relative Euclidean
topology.  A positive-radius conditional tube means open in this relative
topology.

The finite parameter space is a product of compact coefficient boxes and
probability simplices.  An open parameter cell means open in the relative
interior of that product, with fixed cutoff combinatorics and strictly positive
support weights.

## 2.3 One exchangeable initial law

For every physical parameter \(	heta\), declare one law

\[
\mu_\theta(x)
=
\frac{1}{Z_\theta}
\mathbf1_{\mathsf S_\theta}(x)
\rho_\theta(c,\ell,\rho)
\prod_{i=1}^{9}
\eta_\theta(\xi_i\mid c,\ell).
\tag{16}
\]

All directional factors are identical.  The source-valid indicator is
\(S_9\)-invariant.  Hence

\[
\mu_\theta(\sigma x)=\mu_\theta(x)
\qquad
(\sigma\in S_9).
\tag{17}
\]

Neither \(ho_\theta\) nor \(\eta_\theta\) may condition on a response rank, a
number of clear directions or a preferred subset.  Initial mass in every
response cell is measured after the law has been declared.

---

# 3. The complete finite probability generator

## 3.1 Upwind geometry drift

Write the reduced continuous coordinates as

\[
y=(\lambda_1,\ldots,\lambda_9,
    h_1,\ldots,h_9,
    \bar\phi).
\]

At fixed occupations and reservoir, the source vector field is

\[
F_{\lambda_i}=h_i,
\tag{18}
\]

\[
F_{h_i}=\nu h_i+rac{e^{\bar\phi}}{8\pi^2}P_i,
\tag{19}
\]

\[
F_{\bar\phi}=\nu,
\tag{20}
\]

with \(
u\) given by (15).  Between source reactions,

\[
\dot E_0=0.
\tag{21}
\]

For each grid coordinate \(r\), let \(x_r^+\) and \(x_r^-\) be its adjacent
grid states and let \(\Delta_r^\pm\) be the corresponding spacings.  Define

\[
q_{x,x_r^+}^{\rm dg}
=
\frac{[F_r(x)]_+}{\Delta_r^+},
\qquad
q_{x,x_r^-}^{\rm dg}
=
\frac{[-F_r(x)]_+}{\Delta_r^-}.
\tag{22}
\]

If an indicated neighbor is absent or lies outside the source-valid domain,
the same rate is sent to \(\dagger\).  A geometry transition changes no
occupation and no physical reservoir variable.

For a geometry edge \(x\to y\), define the midpoint work quadrature

\[
W_{xy}
=
-\sum_i\frac{P_i(x)+P_i(y)}{2}
       (\lambda_i(y)-\lambda_i(x)).
\tag{23}
\]

Update the numerical residual ledger by

\[
\rho(y)
=
\rho(x)
+W_{xy}
-[E(y)-E(x)].
\tag{24}
\]

If the updated residual leaves its predeclared interval, send the transition to
\(\dagger\).  Thus every accepted finite-volume edge satisfies the exact
bookkeeping identity

\[
E(y)+\rho(y)
=
E(x)+\rho(x)+W_{xy}.
\tag{25}
\]

## 3.2 Exactly one impact semantics

All impact marks are integrated out.  No persistent mark state and no separate
\(\mathcal L_{\rm marks}\) are present.

For each eligible oppositely wound pair in direction \(i\), choose a finite
impact grid \(\mathcal B_i\), a normalized probability vector

\[
\sum_{b\in\mathcal B_i}
\mu_i(b\mid x)=1,
\tag{26}
\]

and an absolute recollision frequency \(
u_i(x)\ge0\).  The thinned accepted
pair hazard is

\[
\gamma_{W,i}^-(x)
=
\nu_i(x)
\sum_{b\in\mathcal B_i}
\mu_i(b\mid x)
\min\{1,P_{\rm GKM}(b\mid x)\}.
\tag{27}
\]

The GKM factor is primary-source derived on its declared source strata.  The
generic anisotropic \(\mu_i\) and \(
u_i\) are new measurable closures.

## 3.3 Winding reactions

The winding-annihilation intensity is

\[
a_{W,i}^-(x)
=
\gamma_{W,i}^-(x)
\,w_i^+w_i^-.
\tag{28}
\]

Its reset is

\[
(w_i^+,w_i^-,\ell)
\longmapsto
(w_i^+-1,w_i^--1,\ell+q_W(R_{a_i})).
\tag{29}
\]

The reverse channel has an independently declared absolute coefficient
\(\gamma_{W,i}^+(x)\ge0\) and unordered-reservoir propensity

\[
a_{W,i}^+(x)
=
\gamma_{W,i}^+(x)
\frac{(\ell)_{q_W(R_{a_i})}}{q_W(R_{a_i})!},
\tag{30}
\]

with reset

\[
(w_i^+,w_i^-,\ell)
\longmapsto
(w_i^++1,w_i^-+1,\ell-q_W(R_{a_i})).
\tag{31}
\]

In the radiation control, \(\gamma_{W,i}^+=0\).  In a Hagedorn control, it must
be present and independently calibrated.

## 3.4 Momentum reactions

The momentum pair channels are

\[
a_{K,i}^-(x)
=
\gamma_{K,i}^-(x)k_i^+k_i^-,
\tag{32}
\]

\[
(k_i^+,k_i^-,\ell)
\longmapsto
(k_i^+-1,k_i^--1,\ell+q_K(R_{a_i})),
\tag{33}
\]

and

\[
a_{K,i}^+(x)
=
\gamma_{K,i}^+(x)
\frac{(\ell)_{q_K(R_{a_i})}}{q_K(R_{a_i})!},
\tag{34}
\]

\[
(k_i^+,k_i^-,\ell)
\longmapsto
(k_i^++1,k_i^-+1,\ell-q_K(R_{a_i})).
\tag{35}
\]

The momentum coefficients are explicit closure parameters unless a primary
source fixes them in the chosen thermodynamic regime.

## 3.5 Cutoff boundary rules

A reaction rate is zero when a reactant or reservoir quantum is absent.  If a
physically enabled reaction would create an occupation above \(N_{\max}\), a
reservoir level above \(L_{\max}\), or a state outside the declared source
regime, its full unsuppressed intensity is sent to \(\dagger\).  It is not
silently reflected or deleted.

Winding charge and momentum charge,

\[
q_{W,i}=w_i^+-w_i^-,
\qquad
q_{K,i}=k_i^+-k_i^-,
\tag{36}
\]

are preserved by all four pair channels.  At fixed geometry every non-cemetery
reaction conserves the exact quantized matter energy (13).

## 3.6 Matrix definition

Let \(q_{xy}^{\rm occ}\) be the sum of all occupation-reaction intensities from
\(x\) to \(y\), including cemetery transitions.  For \(x\ne y\), define

\[
Q_{\theta,xy}
=
q_{xy}^{\rm dg}+q_{xy}^{\rm occ}.
\tag{37}
\]

Set

\[
Q_{\theta,xx}
=
-\sum_{y\ne x}Q_{\theta,xy},
\qquad
Q_{\theta,\dagger\dagger}=0.
\tag{38}
\]

Equations (7)–(38) fully specify the finite Markov generator.  There is no
undefined drift, mark process, reaction or cutoff rule.

## Theorem 1 — finite-generator validity

Assume all finite coefficients are nonnegative and all directional parameter
arrays are permuted covariantly.  Then:

1. every off-diagonal entry of \(Q_\theta\) is nonnegative;
2. every row sums to zero;
3. all non-cemetery reaction jumps conserve directional charges and exact
   quantized matter energy;
4. the numerical work identity (25) holds on every geometry edge;
5. the finite process is nonexplosive;
6. \(\dagger\) is absorbing and records every source, cutoff or residual exit;
7. for every \(\sigma\in S_9\),

   \[
   Q_{\theta,\sigma x,\sigma y}=Q_{\theta,xy};
   \tag{39}
   \]

8. no \(O(9)\) covariance is implied.

### Proof

Items 1–2 follow directly from (37)–(38).  Falling-factorial propensities vanish
when reactants are absent.  Equations (29)–(35) transfer exactly the quantized
pair energy to or from the reservoir and preserve the signed charges.  The
state space is finite, so the total exit rate is bounded and explosion is
impossible.  Simultaneous permutations commute with the source vector field,
impact closure, reactions, cutoffs and ports.  A general continuous rotation
does not preserve the diagonal torus coordinate family.

## 3.7 Finite-volume enclosure

Let \(\iota_h f\) denote sampling of a \(C^2\) continuum test function at grid
centers.  Taylor expansion of the upwind operator gives

\[
\left\|
Q_{\rm dg}\iota_h f
-
\iota_h(F\cdot\nabla f)
\right\|_\infty
\le
\frac12
\sum_r
\Delta_r^{\max}
\|F_r\|_\infty
\|\partial_{rr}f\|_\infty
+
\varepsilon_{\rm cell}(f).
\tag{40}
\]

The term \(arepsilon_{\rm cell}\) is the interval enclosure of coefficient
variation inside each grid cell.  The total deterministic approximation budget
is

\[
\varepsilon_{\rm mod}
=
\varepsilon_{\rm FV}
+
\varepsilon_{\rm impact}
+
\varepsilon_{\rm Gal}.
\tag{41}
\]

No finite response margin may be claimed unless it exceeds
\(arepsilon_{\rm mod}\) after propagation through the relevant semigroup or
resolvent bound.

---

# 4. One augmented operator for probability and response

## 4.1 Probe and product ports

Let \(V_{\rm pr}\) be a finite Fourier/two-jet space containing, for every
anonymous direction,

\[
1,\qquad \cos\theta_i,\qquad\sin\theta_i,
\tag{42}
\]

all products needed through declared degree two, and every declared probe
polarization.  At finite geometry state \(x\), let \(A_x\) be the Galerkin
matrix of the democratic minimally coupled probe action.

A quadratic modulation port is added:

\[
S_{\rm mult}
=
-\frac12
\sum_\alpha
\int dt\,v_\alpha(t)
\int d^9\theta\sqrt{g_9}\,
\chi_\alpha(\theta)
\psi^T K\psi,
\tag{43}
\]

where \(\chi_\alpha\) spans the required products of the zero and first
harmonics.  Mixed source/modulation/readout derivatives produce the
multiplication and character data.  Thus the response quotient is not inferred
from a linear transfer matrix alone.

## 4.2 The augmented matrix

Set

\[
V=\mathbb R e_{\rm prob}\oplus V_{\rm pr},
\]

with

\[
A_xe_{\rm prob}=0.
\]

Every finite Markov transition carries the probe coefficients by the declared
identity/reset map \(R_{yx}\), with

\[
R_{yx}e_{\rm prob}=e_{\rm prob}.
\]

On \(\mathbb R^{\mathsf S_\theta}\otimes V\), define the single finite matrix

\[
(\mathbb Q_\theta Z)_x
=
A_xZ_x
+
\sum_{y\ne x}Q_{\theta,yx}R_{yx}Z_y
-
\left(\sum_{y\ne x}Q_{\theta,xy}\right)Z_x.
\tag{44}
\]

The subspace

\[
\mathbb R^{\mathsf S_\theta}\otimes\mathbb Re_{\rm prob}
\]

is invariant and carries exactly the probability generator \(Q_\theta\).
Therefore the probability and response calculations are restrictions of one
finite operator, not unrelated surrogates.

## 4.3 Linear and mixed transfers

For source vectors \(B_a\), readouts \(C_b\) and modulation operators
\(M_\alpha\), define

\[
G_{ba}(z)
=
C_b(zI-\mathbb Q_\theta)^{-1}B_a,
\tag{45}
\]

and

\[
G^{(2)}_{b;\alpha a}(z_2,z_1)
=
C_b(z_2I-\mathbb Q_\theta)^{-1}
M_\alpha
(z_1I-\mathbb Q_\theta)^{-1}B_a.
\tag{46}
\]

All poles and Laurent coefficients come from \(\mathbb Q_\theta\).  For a simple
eigenvalue \(\zeta_j\),

\[
\mathcal R_j
=
\frac{(Cr_j)(\ell_j^*B)}{\ell_j^*r_j}.
\tag{47}
\]

A zero residue is an unobserved mode.  A repeated eigenvalue uses the complete
Laurent principal part.

## 4.4 Local response cells are killed restrictions

For a finite response-observation cell \(\mathcal C\subset\mathsf S_\theta\),
let

\[
\mathbb Q_{\theta,\mathcal C}
\]

be the restriction of \(\mathbb Q_\theta\) with every exit sent to a cemetery
channel.  The local response experiment uses

\[
C(zI-\mathbb Q_{\theta,\mathcal C})^{-1}B.
\tag{48}
\]

This is derived from the same full operator.  The probability of leaving the
cell during the response window is computed from the corresponding killed
probability block and enters the response-validity margin.

## 4.5 Response-defined rank cells

Fix a bandwidth interval \(I=[\Lambda_-,\Lambda_+]\) and all spectral,
residue, observability, relation and approximation margins before declaring any
output.  Run the complete anonymous relation/Green reconstruction on (45)–(48).

Define

\[
Z_m(\theta,I)
=
\left\{
 x:\text{the complete local response certificate returns visible spatial
 cotangent rank }m
\right\}.
\tag{49}
\]

The sets \(Z_m\) are introduced only after the generator, initial law, ports,
band and errors are fixed.  They are unions of \(S_9\)-orbits.  No state
transition consults them.

The local Green matrices are inverted and the principal biform is obtained by

\[
H_x^\vee(p_{L,x})=\beta_x,
\qquad
\mathcal L_x
=
\mu_{2,x}^\vee(\ker H_x^\vee).
\tag{50}
\]

Ordered inertia is an observed property.  It is not passed to the mechanism.
The declared minimally coupled probes provide an executable common-cone
control, not a universal all-field theorem.

## 4.6 Exact all-rank probability formulas

Let \(P_m\) be the diagonal projector onto \(Z_m\), and let
\(Q_{\bar m}\) and \(Q_m\) be the killed subgenerators on the complement and on
\(Z_m\), respectively.  With row-vector convention,

\[
I_m
=
\mu_\theta P_m\mathbf1,
\tag{51}
\]

\[
H_m(T)
=
1-\mu_\theta P_{\bar m}
   e^{TQ_{\bar m}}\mathbf1,
\tag{52}
\]

\[
A_m(T)
=
H_m(T)-I_m,
\tag{53}
\]

\[
R_m(T_0,T_1)
=
\frac{1}{T_1-T_0}
\mu_\theta
\int_{T_0}^{T_1}
e^{tQ_\theta}P_m\mathbf1\,dt,
\tag{54}
\]

and the worst-case leakage is

\[
L_m(T)
=
1-\min_{x\in Z_m}
e_x^Te^{TQ_m}\mathbf1.
\tag{55}
\]

These expressions include every exit face: occupation, pressure, gap,
source-validity, residue, observability, approximation and cemetery exits.

A robust three-selection margin would require an open relative parameter cell
\(\Theta_*\) with

\[
\inf_{\theta\in\Theta_*}
\left[
R_3(\theta)
-
\max_{m\ne3}R_m(\theta)
-
\varepsilon_R(\theta)
\right]
>0,
\tag{56}
\]

where \(arepsilon_R\) is a deterministic semigroup enclosure or an explicitly
labeled statistical coverage allowance.  An unlabeled norm error is never
added to a probability.

---

# 5. Corrected arbitrary-\(m\) conditional theorem

This section concerns the source continuous geometry inside a forward tube.
It proves a transition lemma, not an all-rank preference.

## 5.1 Correct wound-direction comparison

Assume on a forward interval

\[
-\bar u\le\nu(t)\le-\underline u<0,
\qquad
c(t)=\frac{e^{\bar\phi(t)}}{8\pi^2}\ge\underline c>0,
\tag{57}
\]

and suppose a marginal direction obeys

\[
P_j(t)\le-P_-<0,
\qquad
h_j(0)\le H_+.
\tag{58}
\]

Put

\[
a=\underline cP_->0.
\]

### Lemma 1 — two-stage recollapse bound

If \(h_j(0)\ge0\), define

\[
t_0
=
\frac1{\underline u}
\log\left(1+\frac{\underline u h_j(0)}{a}\right).
\tag{59}
\]

Then \(h_j\) reaches zero no later than \(t_0\).

After the first zero, define

\[
\eta=\frac{a}{2\bar u},
\qquad
 t_1=t_0+\frac{\log2}{\bar u}.
\tag{60}
\]

Then

\[
h_j(t)\le-\eta
\qquad(t\ge t_1)
\tag{61}
\]

as long as (57)–(58) hold.  The region \(h_j\le-\eta\) is forward invariant.

The maximal positive expansion before the first zero is bounded by

\[
I_+
=
\frac{h_j(0)}{\underline u}
-
\frac{a}{\underline u^2}
\log\left(1+\frac{\underline u h_j(0)}a\right).
\tag{62}
\]

For a trap radius \(\lambda_{\rm tr}<\lambda_j(0)\), a valid recollapse time is

\[
\boxed{
T_{\rm rec}
=
 t_1
+
\frac{
 [\lambda_j(0)+I_+-\lambda_{\rm tr}]_+
}{\eta}.
}
\tag{63}
\]

If \(h_j(0)<0\), take the first-zero stage to have length zero and use the same
negative-side comparison from the initial value.

### Proof

While \(h_j\ge0\),

\[
\dot h_j
=
\nu h_j+cP_j
\le
-\underline u h_j-a.
\]

The comparison solution reaches zero at (59).  Once \(h_j\le0\), the correct
upper endpoint is \(ar u\):

\[
\dot h_j
\le
-\bar u h_j-a.
\]

Starting from zero, this comparison reaches \(-a/(2\bar u)\) after
\((\log2)/\bar u\).  At the boundary \(h_j=-\eta\),

\[
\dot h_j\le\bar u\eta-a=-a/2<0,
\]

so upward crossing is impossible.  Integrating the positive comparison before
the first zero gives (62); integrating \(h_j\le-\eta\) then gives (63).

## 5.2 Cleared directions

Assume

\[
P_i(t)\ge P_+>0,
\qquad
h_i(0)\ge0.
\tag{64}
\]

Put \(a_+=\underline cP_+\).  While \(h_i\ge0\),

\[
\dot h_i
\ge
-\bar u h_i+a_+.
\tag{65}
\]

At \(h_i=0\) the right side is positive, so the nonnegative region is forward
invariant.  In particular,

\[
h_i(t)
\ge
h_i(0)e^{-\bar ut}
+
\frac{a_+}{\bar u}(1-e^{-\bar ut})>0.
\tag{66}
\]

Thus a cleared low-gap direction remains expanding while its pressure and
source bounds hold.

## 5.3 Full-horizon occupation bounds

Fix \(m\in\{0,\ldots,8\}\).  Let \(L\) be an unlabeled set of \(m\) cleared
low-gap directions, let \(j_*
otin L\) be one wound marginal low-gap
direction, and let the remaining directions be wound high-gap directions.

Let

\[
T_A
=
\max\{T_{\rm rec},T_{\rm clr}\}+T_{\rm obs}.
\tag{67}
\]

For every trapped direction \(j\notin L\), let \(q_j\) be the number of
annihilations required to leave its winding-pressure buffer and define the
full-tube intensity bound

\[
\bar a_j
=
\sup_{x\in\mathcal U}
a_{W,j}^-(x).
\tag{68}
\]

Then

\[
\epsilon_{\rm trap}(T_A)
=
\sum_{j\notin L}
\Pr\!\left[
{\rm Pois}(\bar a_jT_A)\ge q_j
\right]
\tag{69}
\]

protects all \(9-m\) wound directions over the entire attraction horizon.
Losses are not reset window by window.

For every clear direction \(i\in L\), suppose in the radiation control that
reverse creation vanishes and

\[
a_{W,i}^-(x)\ge\underline\gamma_i n_i
\qquad(n_i>0).
\tag{70}
\]

Then

\[
\epsilon_{\rm clr}(T_{\rm clr})
=
\sum_{i\in L}
n_i(0)e^{-\underline\gamma_iT_{\rm clr}}
\tag{71}
\]

bounds incomplete clearance.  In a regime with reverse creation, either use the
exact killed matrix \(Q_\theta\) or add the full-horizon compensator bound

\[
\epsilon_{\rm cr}(T_A)
=
1-e^{-\bar c_AT_A},
\tag{72}
\]

where \(ar c_A\) is a uniform upper bound for the total reverse-creation
intensity.  No death intensity is assumed to remain bounded by its initial
value.

Let

\[
\epsilon_\dagger(T_A)
=
1-e^{-\bar q_\dagger T_A}
\tag{73}
\]

bound all finite-grid, source-validity and residual-ledger cemetery exits.
Response and approximation conditions are deterministic interval margins; if
they are statistical, their coverage probability is reported as a separate
term \(\alpha_{\rm cov}\).

## Theorem 2 — conditional \(m+1\to m\) response transition

For any \(m\in\{0,\ldots,8\}\), assume:

1. the source forward tube and all deterministic response margins remain valid;
2. the \(m\) clear directions satisfy (64)–(66);
3. the marginal direction satisfies Lemma 1;
4. all \(9-m\) wound directions satisfy the buffer bound (69);
5. the \(m\) clear directions satisfy (71), with reverse creation treated by
   (72) or by the exact killed generator;
6. the local response cell survives for the observation interval and the
   complete relation/Green certificate is valid before and after crossing;
7. the finite model error is smaller than every spectral and relation margin.

Then a response-defined visible-\((m+1)\) predecessor reaches a
response-defined visible-\(m\) cell by time \(T_A\) with probability at least

\[
1-\epsilon_{\rm trap}
 -\epsilon_{\rm clr}
 -\epsilon_{\rm cr}
 -\epsilon_\dagger
 -\alpha_{\rm cov}.
\tag{74}
\]

The exact total tube-exit probability after arrival is (55).  If only the
occupation faces are estimated analytically, the result is explicitly called an
occupation-exit bound rather than a total tube-exit bound.

### Proof

The full-horizon Poisson domination protects the marginal and every other wound
direction until the geometry comparison is complete.  Lemma 1 sends the
marginal first-harmonic gap through the predeclared band.  Lemma 2 preserves the
\(m\) clear low-gap directions.  The clearance and reverse-creation estimates
control their occupation faces.  Every other exit is either ruled out by a
deterministic enclosure or counted by the cemetery/killed generator.  The
response cell therefore changes from \(m+1\) to \(m\) on the complement of the
listed failure events.

### Mandatory counterfactuals

The same theorem and code must succeed for at least

\[
m=2,\qquad m=3,\qquad m=4.
\]

Failure away from \(m=3\) is evidence of a hidden target branch.

---

# 6. The all-rank comparison from one law and one generator

## 6.1 Score vector

For fixed \(	heta\), one initial law (16), one operator (44), one band and one
error model produce the complete vector

\[
\mathbf S_\theta
=
\left(
I_m,
A_m(T),
R_m(T_0,T_1),
L_m(T)
\right)_{m=0}^{9}.
\tag{75}
\]

The output rank is not used to choose a simulation, an initial condition, a
port, a bandwidth or a transition law.

A genuine three-selection certificate requires all of:

1. \(A_3(T)>0\), so the result is not merely initial mass;
2. a positive basin mass outside \(Z_3\);
3. the robust margin (56);
4. a finite leakage bound \(L_3(T)\le\epsilon_*\);
5. robustness to the predeclared band and model-error box;
6. the same finite operator reproducing all first-passage and response data.

No such source-derived margin is currently available.

## 6.2 Equal-hazard control

Consider the exact subfamily in which the nine directional visibility
indicators are independent, identically distributed two-state chains with

\[
0\xrightarrow{\alpha}1,
\qquad
1\xrightarrow{\beta}0,
\tag{76}
\]

and the initial law is product Bernoulli with parameter \(p_0\).  Then

\[
p(t)
=
\frac\alpha{\alpha+\beta}
+
\left(
p_0-rac\alpha{\alpha+\beta}
\right)e^{-(\alpha+\beta)t},
\tag{77}
\]

and the response-defined count is

\[
K_t\sim{\rm Binomial}(9,p(t)).
\tag{78}
\]

Hence

\[
R_k(T_0,T_1)
=
\frac1{T_1-T_0}
\int_{T_0}^{T_1}
{9\choose k}p(t)^k(1-p(t))^{9-k}\,dt.
\tag{79}
\]

At stationarity, with \(p_*=\alpha/(\alpha+\beta)\), rank \(k\) is the unique
mode exactly when

\[
k<10p_*<k+1.
\tag{80}
\]

Thus every \(k\in\{0,\ldots,9\}\) is the unique winner on an open rate-ratio
cell.  Exchangeability and identical local rules do not privilege three.

If a three advantage in this control comes from \(p_0\) and disappears after
burn-in, it is labeled initial-measure bias.

## 6.3 Easther–Greene–Jackson–Kabat all-or-nothing control

On the exactly diagonal subspace

\[
\lambda_i=\lambda,
\quad
h_i=h,
\quad
w_i^\pm=w^\pm,
\quad
k_i^\pm=k^\pm
\qquad(1\le i\le9),
\tag{81}
\]

with identical directional marks, \(S_9\) symmetry keeps all directions equal.
All first-harmonic gaps cross a fixed response band simultaneously.  Therefore
intermediate cells \(Z_1,\ldots,Z_8\) are not visited on this invariant control.
It is an all-or-nothing decompactification control, not a three preference.

## 6.4 Non-three and tie cells

The same finite executable must include:

- a rate cell whose residence winner is \(m=2\);
- a rate cell whose residence winner is \(m=4\);
- an equal-hazard cell with a tie or no separated winner;
- a band perturbation for which the former winner loses its margin.

These controls prevent a plotting or labeling convention from being mistaken
for selection.

---

# 7. The sharp all-rank underdetermination theorem

## 7.1 Source-compatible completion fiber

Let \(\mathfrak C_{\rm src}\) be the finite family of pairs

\[
(\mu,Q)
\]

that satisfy:

1. the same primary-source isotropic hazard restrictions;
2. the same source geometry equations and reaction authorization;
3. the same finite cutoffs, ports, band and errors;
4. \(S_9\)-exchangeability of \(\mu\);
5. \(S_9\)-equivariance of \(Q\);
6. positivity and row-sum zero;
7. no geometry jump at a source reaction.

The anisotropic recollision rates away from the source strata are not fixed by
those conditions.

## 7.2 Residence derivative

For a finite generator \(Q\), define

\[
M_k(Q)
=
\frac1{T_1-T_0}
\int_{T_0}^{T_1}
e^{tQ}P_k\mathbf1\,dt,
\qquad
R_k=\mu M_k(Q).
\tag{82}
\]

For any row-sum-zero perturbation \(\Delta Q\), Duhamel differentiation gives

\[
D R_k(Q)[\Delta Q]
=
\frac1{T_1-T_0}
\int_{T_0}^{T_1}
\int_0^t
\mu e^{sQ}
\Delta Q
e^{(t-s)Q}P_k\mathbf1
\,ds\,dt.
\tag{83}
\]

If this pairing is nonzero, the sign of a sufficiently small perturbation can
increase or decrease \(R_k\).

## Theorem 3 — source-compatible residence underdetermination

Assume that for each nonempty rank cell \(Z_k\) there is at least one reachable
\(S_9\)-edge orbit outside the source isotropic strata such that the pairing
(83) is nonzero.  Then:

1. for every \(k\) there is an arbitrarily small, positivity-preserving,
   \(S_9\)-equivariant perturbation of the anisotropic rates that leaves every
   primary-source restriction unchanged but changes \(R_k\);
2. the ordering of the vector \((R_0,\ldots,R_9)\) is not identified by the
   source restrictions;
3. no uniform positive three-margin can hold over the complete
   source-compatible fiber;
4. if the unconstrained edge orbits permit entry-rate enhancement and exit-rate
   suppression on every \(Z_k\), then for every \(k\) there is a completion for
   which \(Z_k\) is the unique residence winner.

### Proof

Parameterize every positive free rate by

\[
q_e(\varepsilon)
=
q_e(0)e^{\varepsilon\sigma_e},
\tag{84}
\]

where \(\sigma_e\) is constant on \(S_9\)-edge orbits and zero on all
source-constrained edges.  Off-diagonal positivity is automatic and the
diagonal is reset to preserve row sums.  Equation (83) proves the local
statement.  For the global statement, lower all exit rates from a reachable
\(S_9\)-orbit contained in \(Z_k\) and increase its unconstrained entry rates.
In the finite chain the mean residence in that orbit tends to one as the
entry/exit ratio diverges, while all source restrictions remain fixed.

The perturbations used in this proof are not proposed physical laws.  They are
indistinguishable source-compatible completions.  Their existence proves that
the current source data do not determine a winner.

## 7.3 Initial-law underdetermination

If the exchangeable initial law is not independently measured, an additional
ambiguity remains.  Let \(g_k\) be any bounded \(S_9\)-invariant function
supported in \(Z_k\), and set

\[
\mu_\varepsilon(x)
=
\frac{
\mu_0(x)e^{\varepsilon g_k(x)}
}{
\sum_y\mu_0(y)e^{\varepsilon g_k(y)}
}.
\tag{85}
\]

This preserves exchangeability and source-valid support while changing both
\(I_k\) and \(R_k\).  Therefore a residence advantage must be shown to survive
burn-in and must not be attributed solely to the initial law.

## Corollary 3.1 — no source-derived three-selection margin

Within the present source/closure family,

\[
\inf_{(\mu,Q)\in\mathfrak C_{\rm src}}
\left[
R_3-
\max_{m\ne3}R_m
\right]
\le0.
\tag{86}
\]

The correct verdict is `inconclusive` about a preference for three.  This does
not exclude a specific completed microscopic action from selecting three; it
shows that the currently authorized family does not yet identify such an
action or margin.

---

# 8. The smallest measurable non-target-keyed missing term

## 8.1 Finite orbit-rate form

For a finite horizon \(T\), let

\[
\mathcal O_T
\]

be the reachable \(S_9\)-state orbits and let

\[
\mathcal E_T/S_9
\]

be the reachable anisotropic edge orbits.  The minimal finite object that fixes
the all-rank score vector is

\[
\mathcal K_{\rm orb}^{(T)}
=
\left(
\mu(\mathcal O),
 a_e
\right)_{\mathcal O\in\mathcal O_T,\ e\in\mathcal E_T/S_9}.
\tag{87}
\]

If one component with nonzero derivative (83) is left free, the residence
ordering remains underdetermined.  Thus (87) is minimal relative to the chosen
finite state abstraction.

## 8.2 Continuum normal-impact term

For a pair of string histories \(a,b\), let

\[
P_{ab}^{\perp}(X)
\]

be the anonymous projector onto their geometrically defined normal impact
space.  It is computed from their tangents and velocities, not from a response
rank.  Let

\[
\Sigma_{ab}^{\perp}(X)
=
P_{ab}^{\perp}
\Sigma_{ab}(X)
P_{ab}^{\perp}
\tag{88}
\]

be the measured impact covariance.  For a Gaussian impact preparation, the
averaged source factor is

\[
\mathfrak h_{ab}(X)
=
\det{}'
\left(
I+rac{2\Sigma_{ab}^{\perp}(X)}{\Delta_{ab}^2(X)}
\right)^{-1/2}.
\tag{89}
\]

The absolute accepted hazard is then

\[
\bar\gamma_{ab}
=
\nu_{ab}C_{ab}\mathfrak h_{ab}.
\tag{90}
\]

Every quantity in (88)–(90) is measurable from recollision times, impact marks,
geometry and accepted-event counts.  No dimension label or target cardinality
appears.

A microscopic action that derives \(\Sigma_{ab}^{\perp}\), rather than merely
fitting it, would close the generator ambiguity.  Once (87) or its continuum
equivalent is fixed, the finite formulas (51)–(56) either certify an all-rank
three margin or exclude it for that model.

## Theorem 4 — minimality of the anisotropic kernel

Data restricted to the isotropic source strata cannot determine
\(\mathcal K_{\rm orb}^{(T)}\).  Any smaller object that omits a reachable
anisotropic edge orbit with nonzero derivative (83) cannot determine the
residence ordering.  Therefore the normal-impact/recollision kernel together
with the exchangeable initial orbit weights is the smallest measurable
non-target-keyed completion of the present finite model.

---

# 9. Joint forward compatibility and response falsifiability

## 9.1 First passage and leakage

All attraction and leakage predictions are computed from \(Q_\theta\):

\[
H_m(T)
=
1-\mu P_{\bar m}e^{TQ_{\bar m}}\mathbf1,
\tag{91}
\]

\[
L_m(T)
=
1-\min_{x\in Z_m}e_x^Te^{TQ_m}\mathbf1.
\tag{92}
\]

No fitted birth–death surrogate replaces the finite generator.

## 9.2 Response and departure

Every linear and mixed response is computed from the same augmented matrix
\(\mathbb Q_\theta\) by (45)–(46).  A killed restriction is used only for a
predeclared hold or exit event and is explicitly derived from the full matrix.

The exact nonstationary response is the two-time propagator

\[
G_{ba}(t_2,t_1)
=
C_b e^{(t_2-t_1)\mathbb Q_\theta}B_a.
\tag{93}
\]

A quasi-stationary reduction may be used only if a resolvent or semigroup error
is enclosed.  For a decomposition

\[
\mathbb Q_\theta
=
\mathbb A+\varepsilon\mathbb S
\]

and a spectral point at distance \(d\) from
\({\rm spec}(\mathbb A)\), the resolvent identity gives, when
\(arepsilon\|\mathbb S\|<d\),

\[
\left\|
(zI-\mathbb Q_\theta)^{-1}
-(zI-\mathbb A)^{-1}
\right\|
\le
\frac{
\varepsilon\|\mathbb S\|
}{d(d-\varepsilon\|\mathbb S\|)}.
\tag{94}
\]

This error must fit inside the pole and residue margins.

The model is jointly falsified if no one \(	heta\) reproduces:

- absolute accepted and reverse event counts;
- geometry-bin transition statistics;
- all-rank hitting and residence scores;
- finite-horizon leakage;
- observable poles and Laurent residues;
- mixed/product response data;
- response-band crossings.

The word `identifiable` is used only if the complete parameter-to-data map is
proved injective modulo the registered gauge.  Otherwise the result is a joint
compatibility and falsifiability test.

---

# 10. Mandatory all-rank adversaries

## 10.1 Hidden target branch

Insert a rate, initial-law weight, geometry potential or cutoff that changes
when a count equals three.

**Required result:** static non-encoding audit rejects the model.

## 10.2 Direct geometry or rank reset

Allow a winding event to change a radius, velocity, response relation or
projector instantaneously.

**Required result:** source authorization and generator audit fail.

## 10.3 Conditional-lemma cross-check

Run exactly the same code and parameter conventions for

\[
m=2,\quad3,\quad4.
\]

**Required result:** each conditional theorem works when its own physical tube
hypotheses hold.  Failure only away from three reveals target coding.

## 10.4 Equal-hazard control

Use (76) with exchangeable product initial data.

**Required result:** report the binomial score vector (79).  Any three advantage
caused by \(p_0\) is initial-measure bias.

## 10.5 All-or-nothing control

Use the diagonal invariant sector (81).

**Required result:** no intermediate-rank preference is reported.

## 10.6 Non-three winners and ties

Use physical rate cells producing rank-two, rank-four and tied winners.

**Required result:** the all-rank comparison reports the actual winner or no
separated winner.

## 10.7 Band/error perturbation

Enlarge the predeclared band or approximation box until a residue, relation or
gap margin closes.

**Required result:** the winner changes or the answer becomes `inconclusive`.
No tolerance fills an unproved interval.

## 10.8 Quotient-only observability

Delete one pre-quotient port direction while keeping the low-band response
complete.

**Required result:** microscopic non-hiding is unresolved.  The claimed
observable sector is stated exactly; no full-state Hautus claim is made.

## 10.9 Different probability and response operators

Fit residence with one matrix and transfer poles with another.

**Required result:** joint-operator audit rejects the model.

## 10.10 Zero-residue mode

Include an eigenvalue annihilated by the source or readout.

**Required result:** it is not counted as an observed response direction or
departure.

## 10.11 Retarded boundary smuggling

Use a retarded probe Green function and call its support a dynamically selected
time arrow.

**Required result:** the time-selection claim is rejected.  The source
cosmology supplies its time branch in this executable.

---

# 11. Closed-loop verdict semantics

Let \(\mathfrak M_D\) be the raw finite microscopic candidate fiber defined only
by data consistency, source validity and predeclared complexity bounds.  A
three-winning residence score is not a membership condition.

For every candidate \(M\), distinguish

\[
\mathsf T_{\rm all}(M)
=
\text{the actual completed model has a robust all-rank three margin},
\tag{95}
\]

and

\[
\mathsf R_{\rm all}(M,D)
=
\text{the registered finite route proves that margin and all required bounds}.
\tag{96}
\]

If \(\mathfrak M_D=\varnothing\), report model-class/data incompatibility
separately.  For a nonempty fiber:

1. `certified within class` requires
   \(\mathsf T_{\rm all}\wedge\mathsf R_{\rm all}\) for every candidate,
   complete response/coverage artifacts and one full microscopic/response
   orbit;
2. `class-excluded` requires an actual or positively separated obstruction for
   every candidate;
3. every other case is `inconclusive`.

Theorem 3 supplies an obstruction to certification over the present incomplete
source/closure fiber: it contains completions with different residence winners.
It does not exclude all future completed actions from selecting three.

---

# 12. Finite executable

Use one finite parameter box with:

1. nine radius and velocity grids;
2. winding and momentum occupations in \(\mathbb N_0\) up to \(N_{\max}\);
3. a quantized reservoir and numerical residual ledger;
4. the integrated thinned impact kernel (27);
5. all four pair-reaction channels (28)–(35);
6. one cemetery state for every cutoff and source exit;
7. two minimally coupled probe species;
8. zero, first and degree-two harmonic ports;
9. one predeclared bandwidth family;
10. one \(S_9\)-exchangeable initial law (16).

The implementation must:

1. construct every entry of \(Q_\theta\) and \(\mathbb Q_\theta\);
2. verify off-diagonal positivity, row sums and \(S_9\) covariance exactly;
3. compute all \(Z_m\), \(m=0,\ldots,9\), only after response replay;
4. compute (51)–(55) for every \(m\);
5. report initial mass and attraction separately;
6. run the arbitrary-\(m\) theorem for \(m=2,3,4\);
7. run the equal-hazard and all-or-nothing controls;
8. optimize the robust margin (56) over the complete parameter/error box;
9. evaluate every pole and residue of the same augmented matrix;
10. replay every adversary in Section 10.

The finite theorem is exact for the declared matrix and interval enclosures.
Convergence to the continuous cosmology remains a controlled numerical
conjecture governed by (40)–(41).

---

# 13. Evidence ledger

| classification | result |
|---|---|
| `primary-source derived` | Anisotropic shifted-dilaton drift; winding/momentum energy and pressure signs; fixed-d GKM impact amplitude and source reaction authorization; conditional Hagedorn scenario. |
| `derived from a newly proposed action` | Integrated anisotropic impact closure; exact reservoir reaction lift; finite-volume geometry operator; democratic probe and quadratic modulation ports. |
| `exact theorem about a declared model` | Complete finite generator; conservation and symmetry theorem; one-operator score and response formulas; corrected arbitrary-\(m\) theorem; equal-hazard binomial control; source-compatible all-rank underdetermination; minimal orbit-rate data theorem. |
| `controlled asymptotic/numerical conjecture` | Finite-volume convergence; finite anisotropic use of the GKM amplitude; probe/Galerkin convergence; source derivation of the exchangeable initial law. |
| `no-go/underdetermination` | Exchangeability does not select cardinality; isotropic source restrictions leave residence ordering free; equivalent open winner cells exist for all ranks in the equal-hazard control; the conditional lemma does not select three. |
| `open gate` | Derive or measure the complete normal-impact kernel and initial orbit weights; derive primitive string-orbit dominance, a universal all-field carrier and a physical time orientation. |

---

# 14. Minimal theorem and required conclusion

> **Exchangeable all-rank selection-gate theorem.**  
> Let the finite winding–geometry model be defined by (7)–(44), with one
> exchangeable initial law, one equivariant finite generator, one democratic
> pre-quotient linear/mixed port system and one predeclared bandwidth/error
> family.  Let \(Z_m\) be introduced only after anonymous reconstruction.
>
> Then all rank hitting, attraction, residence and leakage quantities are given
> exactly by (51)–(55), and all response poles and residues are derived from the
> same augmented operator.  For every \(m=0,\ldots,8\), the physical tube
> hypotheses of Theorem 2 imply a valid conditional visible-\((m+1)	o m\)
> transition with the corrected geometry and full-horizon probability bounds.
>
> However, if the anisotropic recollision/impact rates and the exchangeable
> initial orbit weights are constrained only by the currently available
> isotropic source restrictions, Theorem 3 applies.  The residence ordering is
> underdetermined, and no uniform source-derived three-selection margin follows.
> Exchangeability removes preferred axes but does not prefer the cardinality
> three.
>
> The smallest finite missing object is the reachable orbit-rate and initial
> orbit-weight vector (87), whose continuum counterpart is the measurable
> normal-impact covariance kernel (88)–(90).  Once that target-neutral object is
> derived or measured, the all-rank margin (56) becomes a finite theorem or a
> finite exclusion test for the completed model.

This is conclusion 2 required by Brief 0010:

> **The project now has a complete arbitrary-\(m\) conditional model and a sharp
> no-go for three-selection in the current source/closure family.**

The remaining hard gate is to derive from a duality-complete microscopic action
one exchangeable initial fluctuation law and the full anisotropic normal-impact
kernel, and then prove that their all-rank residence vector has a robust
three-margin.  Closing the complete \(3+1\) loop still additionally requires a
primitive stringlike orbit gap, a universal all-field carrier and a physical
retarded or entropy-current time component.

---

# References

1. A. A. Tseytlin and C. Vafa, *Elements of String Cosmology*,
   arXiv:hep-th/9109048.
2. Richard Easther, Brian R. Greene, Mark G. Jackson and Daniel Kabat,
   *String Windings in the Early Universe*, arXiv:hep-th/0409121.
3. Brian Greene, Daniel Kabat and Stefanos Marnerides,
   *Dynamical Decompactification and Three Large Dimensions*,
   arXiv:0908.0955.
4. Brian Greene, Daniel Kabat and Stefanos Marnerides,
   *On Three Dimensions as the Preferred Dimensionality of Space via the
   Brandenberger–Vafa Mechanism*, arXiv:1212.2115.
5. Mairi Sakellariadou, *Numerical Experiments in String Cosmology*,
   arXiv:hep-th/9511075.
