# Brief 0016 — source-to-return kinematic-rank gate

Status: active

Baseline commit: `c73fc0e8b6286c281b8577a9879de5e7c15fdc4a`.

## Role

Act as the originating mathematical physicist and computational co-theorist.
This is the first physical-closure round after the fixed finite certificate was
closed.  Do not review Brief 0015 again, enlarge its verifier, discuss journals,
or divide the program into papers.

Read, in order:

1. `state/PROJECT_STATE.md`;
2. `responses/0012_target_neutral_encounter_recollision_kernel.md`;
3. `decisions/0012_adopt_source_factorization_boundary_not_physical_kernel.md`;
4. `responses/0015_repair_replay_and_nonencoding_boundary.md`;
5. `decisions/0015_adopt_fixed_registered_certificate_not_physical_closure.md`;
6. this brief.

Use primary sources, in particular:

- Greene--Kabat--Marnerides, *Dynamical Decompactification and Three
  Large Dimensions*, arXiv:0908.0955,
  DOI:10.1103/PhysRevD.82.043528;
- Greene--Kabat--Marnerides, *On three dimensions as the preferred
  dimensionality of space via the Brandenberger--Vafa mechanism*,
  arXiv:1212.2115, DOI:10.1103/PhysRevD.88.043527;
- Jackson--Jones--Polchinski, *Collisions of Cosmic F- and D-strings*,
  arXiv:hep-th/0405229, DOI:10.1088/1126-6708/2005/10/013;
- Easther--Greene--Jackson--Kabat, *String windings in the early
  universe*, arXiv:hep-th/0409121,
  DOI:10.1088/1475-7516/2005/02/009;
- Danos--Frey--Mazumdar, *Interaction Rates in String Gas Cosmology*,
  arXiv:hep-th/0409162, DOI:10.1103/PhysRevD.70.106010.
- Alexander--Brandenberger--Easson, *Brane Gases in the Early Universe*,
  arXiv:hep-th/0005212, DOI:10.1103/PhysRevD.62.103509.

Do not infer more from a source than it states.  In particular, a numerical
recollision prescription is not a first-principles return theorem, and a
crossing-conditioned amplitude is not an absolute encounter law.

## The correction this round must make

The previous finite constructor uses an ordered frame of fixed input arity.
For the ordinary instance this is the triple `(w,v,s)`.  Its arity is not a
dynamical state:

- frame enumeration is performed after reading `frame_arity`;
- all frame axes must pass the registered source-radius predicate;
- the complement of those axes supplies the sparse impact marks; and
- every event preserves the chosen arity sector.

Consequently fixed arity three simultaneously causes the onset

\[
\#\{i:R_i>R_{\rm sd}\}\ge 3
\]

for any valid ordinary frame and changes the dimension of the registered
impact complement.  This may become a real physical mechanism if the three
local directions are derived before any response rank is defined.  Until that
derivation exists it is an input at exactly the place where a cardinality
threshold can enter.

The response must never identify the following three integers:

\[
\boxed{
p=\text{spatial dimension of the microscopic carrier},\qquad
a=\text{local encounter kinematic rank},\qquad
m=\text{anonymous response-visible spatial rank}.
}
\]

For an F1 string \(p=1\).  A generic local two-string encounter may have
\(a=3\).  Neither statement says \(m=3\).

## Part I — derive or obstruct the local kinematic rank

Let two local \(p\)-brane embeddings in a spatial metric \(G\) be

\[
X_1(\sigma,t),\qquad X_2(\sigma',t).
\]

Use the difference map

\[
F(\sigma,\sigma',t)=X_1(\sigma,t)-X_2(\sigma',t)
\]

or an equivalent invariant encounter map.  Its derivative has columns
generated by the two tangent spaces and relative motion:

\[
DF=\bigl[D_\sigma X_1,\,-D_{\sigma'}X_2,\,
v_{\rm rel}\bigr].
\]

Do all of the following.

1. Define the coordinate-independent kinematic rank
   \(a(x)=\operatorname{rank}DF_x\), with \(a\le 2p+1\).
2. Prove the general-position statement and list the actual degeneracies.
   For F1, determine precisely when \(a=3\), \(a=2\), or lower.
3. Give a quantitative open-condition margin.  One acceptable form is a
   metric singular-value or Gram-determinant bound
   \[
   \sigma_{2p+1}(G^{1/2}DF_x)\ge\eta_{\rm kin}>0.
   \]
   State how much perturbation of tangents, velocity and metric this margin
   tolerates.
4. Define the impact normal space or normal bundle
   \[
   N_x=(\operatorname{im}DF_x)^{\perp_G}
   \]
   and prove its dimension in a \(d\)-dimensional spatial source geometry.
5. Explain exactly how the source statement that two string worldsheets
   generically intersect in at most four spacetime dimensions becomes
   \(a=3\) and an impact space of dimension \(d-3\), without inserting a
   desired visible rank.
6. Generalize the statement to \(p\)-branes.  The result must make clear why
   the familiar \(2p+1\) ceiling is a carrier/encounter fact rather than a
   theorem about response-visible dimension.

If a source-faithful derivation of all three previous role labels
`(w,v,s)` is impossible, replace them.  A pair of string tangents plus relative
velocity is preferable to retaining a role name that has no invariant
microscopic meaning.

## Part II — prove the fixed-arity superselection boundary

Treat all arity-\(a\) registered constructors as a disjoint union.  Prove or
refute:

> If the event rows have no transition changing the arity label, then the
> arity observable \(A\) is conserved:
> \[
> \Pr(A_t=a)=\Pr(A_0=a).
> \]
> Hence the present finite process cannot dynamically select arity three from
> an arity-neutral open basin.  Any arity-three output is conditional on the
> selected constructor or initial sector.

Also prove the exact current onset formula.  If \(q\) physical directions pass
the radius predicate and frames are ordered distinct \(a\)-tuples, then

\[
N_{\rm valid}(q,a)=
\begin{cases}
q!/(q-a)!,&q\ge a,\\
0,&q<a.
\end{cases}
\]

Classify this honestly:

- it is not automatically hidden target encoding;
- it is not evidence for visible dimension;
- it becomes source-grounded only if \(a\) is independently output by the
  local encounter geometry; and
- even after that, the response cells \(Z_m\) and the all-rank residence gate
  must be run.

## Part III — replace role counting by impact-codimension dynamics

The GKM source mechanism is dimension-sensitive through the impact vector,
not through the name or number of bookkeeping roles.  In the dilute
semiclassical regime use

\[
P(b\mid X)=P_0(X)
\exp\!\left[-\frac{\|b\|^2_{G_N}}{\Delta^2(X)}\right],
\qquad b\in N_x,
\]

with the source-validity and normalization conditions made explicit.

Do not silently assume an impact preparation.  At minimum compare two
predeclared, target-neutral controls for \(c=\dim N_x\):

1. uniform measure in a \(c\)-ball of radius \(r\);
2. independent uniform coordinates in a \(c\)-box of half-width \(r\).

Put \(\rho=r/\Delta\), define \(g_0(\rho)=1\), and derive

\[
g_c^{\rm ball}(\rho)
=c\int_0^1u^{c-1}e^{-\rho^2u^2}\,du
=\frac{c}{2}\rho^{-c}
\gamma\!\left(\frac c2,\rho^2\right)
\]

and

\[
g_c^{\rm box}(\rho)
=\left[
\frac{\sqrt\pi}{2\rho}\operatorname{erf}\rho
\right]^c.
\]

Prove the relevant monotonicity and large-\(\rho\) bounds rather than only
plotting them.  State which facts survive changing the impact preparation and
which are closure-dependent.

For a generic F1 encounter in an effectively \(m\)-dimensional large
subspace, recover \(c=(m-3)_+\) only as a restriction/corollary of the
full-metric construction.  The source-to-return compiler may receive the
metric, tangents, velocities and world-sheet state.  It may not receive
`m`, a response rank, a visible count, a band label or a target stratum.

## Part IV — distinguish a three-ceiling from exact three-selection

Prove the strongest valid statement about the lower side.

The impact-codimension mechanism can distinguish \(m>3\) from generic
\(m\le3\), but by itself it gives no macroscopic impact coordinate for any
\(m\le3\).  Determine whether this establishes only

\[
m\le3
\]

as a decompactification ceiling, or whether an additional source-grounded
term gives a strict \(m=3\) winner.

The 2012 successive-fluctuation model may be used as a candidate lower-side
mechanism, but its own assumptions must remain visible: fluctuation statistics,
timescale, initial preparation, coupling window and homogeneity.  Do not turn
its numerical preference into a theorem over unspecified fluctuation laws.

State a quantitative sufficient condition for a strict rank-three margin.  It
should separate:

\[
\text{lower-side growth/preparation advantage}
\quad\text{from}\quad
\text{upper-side impact-codimension suppression}.
\]

Then identify which factor is primary-source derived, which is a declared
closure, and which remains unmeasured.  If the lower-side factor is
unidentified, conclude sharply that this round derives a stable
three-ceiling, not exact three-selection.

## Part V — specify the minimal repaired physical interface

The old map

\[
v_{\rm registered}\longmapsto K_v
\]

is certified only for one fixed registered contract.  The missing upstream
object is a covariant source-to-return law.  Specify

\[
\mathscr P_X
\bigl(
d\tau,db,dc,dh',dX',d\Delta E_R,d\dagger
\mid h
\bigr),
\]

where \(X\) is the full source state and the outputs include:

- absolute first-hit or return time;
- continuous full-metric impact/orientation data;
- conditional reaction channel;
- post-miss history;
- updated source state;
- reservoir transfer and a reference-measure reverse event; and
- a source-invalid killed outcome.

The interface must output its kinematic rank from the invariant encounter
map.  `frame_arity` is forbidden as an input.

The preparation law \(\mu_0\) remains a second independent object.  Give its
reference measure and explain why uniformly weighting whatever finite states a
discretizer happens to create is not a continuum preparation law.

For this round it is acceptable to prove that the cited sources do not identify
all components of \((\mu_0,\mathscr P_X)\).  It is not acceptable to fill them
with new constants and call the result source-derived.

## Part VI — executable probe

Add a small readable standard-Python artifact under `artifacts/0016/` that:

1. evaluates the ball and box suppression factors for \(c=0,\ldots,6\) on a
   declared \(\rho\) grid;
2. verifies their monotonic ordering numerically at higher precision than the
   reported table;
3. emits the exact valid-frame onset table
   \(N_{\rm valid}(q,a)\) for \(q=0,\ldots,9\) and \(a=1,\ldots,4\);
4. includes full-rank, near-degenerate and degenerate F1 Gram-matrix controls;
5. contains no target-rank branch; and
6. writes one deterministic JSON report with formulas, tolerances and source
   classification.

This is a theorem/probe artifact, not a replacement for the physical return
law and not another hostile software-certificate campaign.

## Interface to the existing response program

End with the exact next composition:

\[
(\mu_0,\mathscr P_X)
\longrightarrow
\text{age-augmented scheduled/return process}
\longrightarrow
\text{anonymous }A,R,B,C,M
\longrightarrow
Z_0,\ldots,Z_9
\longrightarrow
\text{entrant strict residence and leakage}
\longrightarrow
\text{visible cone/signature package}.
\]

Do not count source-large axes and call that count the observed rank.  The
later \(m\) must still be reconstructed from anonymous response.

The present F1/\(T^9\) source model also assumes a Lorentzian \(9+1\)
background time.  Therefore this branch can explain, at most, why three
spatial directions become stably visible/large.  It does not derive the one
time direction.  Keep the time/signature gate separate.

## Required conclusion

End with all of the following, at the strongest proved level:

1. a local kinematic-rank theorem or a precise obstruction;
2. the fixed-arity superselection theorem;
3. an impact-codimension suppression theorem;
4. an explicit verdict: `three-ceiling` or `strict three-selection`;
5. the smallest still-unidentified component of
   \((\mu_0,\mathscr P_X)\); and
6. the next falsifiable calculation connecting the physical law to anonymous
   response rank.

Classify each substantive claim as one of:

- `primary-source derived`;
- `exact theorem about a declared geometric model`;
- `derived from a newly proposed measurable closure`;
- `controlled numerical probe`;
- `no-go/underdetermination`; or
- `open gate`.

## Delivery contract

Write the full result to
`responses/0016_source_to_return_kinematic_rank_gate.md` and the executable
probe/report under `artifacts/0016/`.  Commit the result to the
`brief-0016` branch of `ccyyyYyzz/cyz_n`.  Do not alter the frozen 0015
certificate files and do not claim visible \(3+1\) closure.
