# Brief 0004 — Build the cross-candidate end-to-end visible 3+1 certificate

Status: active

## Role

Act as the originating mathematical physicist. Read, in order:

1. `state/PROJECT_STATE.md`;
2. `decisions/0002_adopt_jet_null_principal_route.md`;
3. `decisions/0003_adopt_contextual_loewy_boundary.md`;
4. `responses/0003_artin_jet_to_event_branch.md`;
5. `decisions/0004_adopt_regular_branch_lift.md`.

Do not review the project. Produce the strongest correct master theorem and
finite certificate that composes the adopted layers.

## Adopted chain

The declared experiment has three independent completeness certificates:

\[
\text{context closure},
\qquad
\text{relation/target-carrier completeness},
\qquad
\text{physical branch coverage}.
\]

Under them, a regular physical branch \(B_0\) and the contextual Loewy object
are linked by

\[
\Theta_{x,2}:J_x^2\twoheadrightarrow\widehat J_{\widehat x}^2.
\]

The comparison must be an isomorphism before the operational quadratic fiber
is called the branch fiber. Centered source-module gates give \(\beta_x=0\).
For \(d=4\), rank nine of the jet-null map identifies one conformal principal
line. A Paper-I visible \(3+1\) verdict additionally requires Lorentz inertia,
one strong cone for every accessible field, tangent-compatible chart overlaps
and a consistent directed time half-cone.

## Exact bottleneck

Response 0003 gave a correct exact quotient theorem but its noisy theorem did
not prove that every interval-consistent candidate has the same presentation.
Equal ranks and Hilbert functions do not exclude two inequivalent smooth
carriers inside the same data intervals. Its Wedin statement also used the
full row space of a noisy matrix instead of a selected singular subspace.

We need a uniform theorem over the entire interval-consistent candidate set,
not a sequence of nominal rank checks.

## Required output

1. Define one compact, gauge-quotiented candidate class containing response
   realizations, bounded branch presentations, physical covers, Green-jet
   maps, multi-field symbols, overlap cocycles and directed response data.

2. State an exact necessary-and-sufficient or sharp sufficient condition for
   **cross-candidate presentation equivalence**. Distinguish equality after a
   registered affine/filtered generator isomorphism from merely equal Hilbert
   functions, dimensions or local jets. Give a finite witness system for two
   inequivalent compatible presentations.

3. Give the correct noisy subspace theorem. Specify the rank selected from a
   nominal matrix, the top-\(r\) right singular subspace, the gap to the tail,
   calibrated norms and a Davis--Kahan/Wedin-type bound that is actually valid
   when the raw noisy matrix is full rank.

4. Compose, in strict logical order:

   \[
   \text{raw contexts}
   \to
   \widehat J^2
   \to
   (B_0,J_x^2)
   \xrightarrow{\Theta\simeq}
   \widehat J^2
   \to
   \ker H_x^*
   \to
   [q_x]_{m Lor}
   \to
   \mathsf P_x=q_x\otimes K_x
   \to
   \text{overlap-glued cone}
   \to
   \text{time orientation}.
   \]

   At every arrow state its independent assumption, exact failure witness and
   numerical separation margin.

5. Prove a three-verdict master theorem over all candidates:

   - `certified within class` only when every consistent candidate has an
     equivalent physical branch and the full composed visible \(3+1\)
     structure;
   - `class excluded` only when no consistent candidate has the target
     structure;
   - `inconclusive` otherwise.

6. Treat neighborhood semantics exactly. Only tangent-frame chart overlaps
   may certify conformal gluing. A connection is an additional law; its
   curvature holonomy is not an overlap-cocycle failure. In four dimensions,
   ordered inertia \((1,3)\) orients the metric line but does not choose a time
   orientation.

7. Define a discrete directed-response certificate for the time half-cone.
   State component-purity, timelike/null margins, overlap consistency and the
   global no-flip condition. Do not infer causality or global hyperbolicity.

8. Give an anonymous-generator four-dimensional exact positive control. Its
   inference API may receive only response values, product/context matrices,
   calibrated norms, cover/overlap witnesses and directed-response records.
   Latent coordinates may generate synthetic data but may not enter any
   certification function.

9. Run at least these independent adversaries:

   - one physical panel point removed;
   - target-carrier/Zariski misalignment such as one branch of \((xy)\);
   - a delayed context direction beyond the tested depth;
   - one contextual quadratic direction collapsed, so \(\Theta\) is not
     injective;
   - rank-eight rather than rank-nine Green coverage;
   - a resolved Euclidean or \((2,2)\) principal line;
   - trace-only or determinant-only multi-field data;
   - an overlap map preserving the ray but not induced by tangent congruence;
   - a directed response with a cone-component flip;
   - two inequivalent smooth presentations inside one interval box.

10. End with a minimal Paper-I theorem statement, a dependency graph and a
    concrete implementation kill test. Separate exact proved content from
    finite-noise architecture still requiring compactness or optimization
    machinery.

## Prohibited shortcuts

- do not use \(n_J\) as an event relation, dimension or regularity bound;
- do not call finite sample density physical branch coverage;
- do not infer one presentation from common ranks or Hilbert functions;
- do not compare noisy subspaces using the full row space of a full-rank noisy
  matrix;
- do not use a nonlinear polynomial generator change at fixed degree unless
  the filtration is transported;
- do not infer regularity from a free quadratic layer;
- do not use determinant factorization as the strong multi-field common-cone
  condition;
- do not replace overlap maps by arbitrary point-to-point transport;
- do not label a time-oriented cone causal or globally hyperbolic;
- do not let latent coordinates enter the inference API.

## Delivery — write directly to GitHub

Write the completed response directly to
`ccyyyYyzz/cyz_n` as

`responses/0004_end_to_end_3plus1_certificate.md`.

Commit to `main` if permitted. Otherwise create branch `gpt/brief-0004` and
open a pull request. Do not return only a downloadable attachment, and do not
edit state, briefs or decisions.

## Decision criterion

The result succeeds only if its quantifiers range over all interval-consistent
candidates and it proves the entire composition. A nominal positive toy plus
independent pointwise rank checks is not an end-to-end certificate.

