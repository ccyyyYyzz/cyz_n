# Brief 0009 — source-authorized winding/geometry hybrid with response-derived visible rank

Status: active

Baseline commit: `ed0e592aa4a36abc5ce754a7d07701e1e5730f86`.

## Role

Act as the originating mathematical physicist, not as a reviewer.  Response
0008 is retained only as a conditional underdetermination theorem and a
negative design specification.  Originate the next constructive object, or
prove the sharpest obstruction if construction is impossible.

Read, in order:

1. `state/PROJECT_STATE.md`;
2. `responses/0007_source_derived_generator.md`;
3. `decisions/0007_adopt_dcs_as_mesoscopic_hypothesis.md`;
4. `responses/0008_fundamental_gate_or_nogo.md`;
5. `decisions/0008_adopt_as_conditional_underdetermination.md`;
6. this brief.

The project remains one closed loop.  Do not discuss journals or split the
program into papers.

## Non-negotiable correction

Do **not** make dimension or projector rank a stochastic state with a reset
such as \(s\to s\pm1\).  The audited GKM source computes wound-string
annihilation at fixed \(d\); it does not authorize a dimension jump.

The microscopic/mesoscopic state must instead contain anonymous directional
geometry and source degrees of freedom, for example

\[
(\lambda_i,\dot\lambda_i,\varphi,\dot\varphi,W_i,\bar W_i,N_i,E_{\rm res},\ldots),
\qquad i=1,\ldots,9,
\]

with no supplied visible-rank variable.  Collision events may change only
occupations and other quantities explicitly allowed by the source-derived
reaction.  Geometry must evolve continuously under one declared
dilaton/gravity system with energy accounting.

Time is supplied in this subproblem by the source cosmology.  Do not promote
this construction into a theorem selecting one time direction.

## Required hybrid generator

Give one mathematically complete PDMP, stochastic differential system, or
piecewise-deterministic Markov generator containing both:

1. the Liouville/drift part for anisotropic scale factors and shifted dilaton;
2. normalized source-authorized winding annihilation/loop-production events
   with the finite-width GKM hazard, occupation combinatorics, marks, reverse
   channels if physically required, and reservoir resets.

State the exact domain, constraints, boundary behavior, conserved or
dissipated quantities, nonexplosion conditions, and anonymous permutation
symmetry actually present.  Do not claim \(O(9)\) symmetry from identical
coordinate potentials.  Separate primary-source equations from proposed
closures and controlled asymptotics.

If the GKM equations are only isotropic at fixed \(d\), identify the smallest
anisotropic extension whose terms are source justified.  If no such extension
is available, prove that obstruction rather than silently inventing one.

## Visible rank must be an output

Define a target-neutral operational response/readout from the hybrid state.
It may use perturbations and the complete pre-quotient Green/resolvent data,
but it must not receive:

- a rank or dimension label;
- a preferred set of three directions;
- a metric, cone flag, or time vector;
- a threshold chosen because the desired answer is three.

Specify how a finite anonymous reconstruction would infer a visible tangent
rank and field biforms at each scale.  The response should interface with the
existing project contract:

1. exact finite relation quotient;
2. registered Green-derived biforms;
3. strict ordered inertia only as an observed property, not a target input;
4. carrier and physical scale maps with non-hiding provenance;
5. no claim that finite-sample rank is intrinsic dimension.

If the source variables do not determine any such response without a new
port/action term, prove the underdetermination and propose the smallest
measurable, non-target-keyed port coupling.

## Selection question

For one finite, executable parameter family, determine whether the same
hybrid can produce all of the following without a rank-keyed controller:

1. a resolved non-three-dimensional predecessor;
2. a positive-width interval on which the operationally inferred visible
   spatial rank is three;
3. quantified leakage/escape probability and attraction time;
4. robustness on an open parameter cell rather than one tuned trajectory;
5. a declared failure result for the same model at other parameter points.

An exact rank-three invariant phase is not required if the source permits
only finite-width positive rates.  In that case formulate the correct
metastable/concentration theorem and give its finite-time error budget.  A
proof that the source-faithful model cannot yield even such a plateau is also
a successful result.

## One joint departure operator

Do not append an independent response matrix.  Linearize or lift the same
joint hybrid generator and derive the observable resolvent, poles/Laurent
coefficients, and residues from it.  State an identifiability condition and a
falsifiable relation among absolute event counts, geometry relaxation,
visible-rank leakage, and response bandwidth.  Treat a zero residue as an
unobserved mode and handle the \(g=0\) removable-point limit correctly.

## Species and time boundaries

Keep the primitive-stringlike charge-orbit problem explicit.  You may fix one
audited charge sector for this executable, but then label species selection
as unresolved.  Do not use literal \(p=1\) as a duality-invariant conclusion.

For ordered time, give only the interface that a later module must close:
retarded versus advanced boundary selection, or a non-equilibrium entropy
current with a response margin.  Onsager reciprocity alone is not a time-arrow
no-go because a retarded scalar kernel already carries directed support.

## Mandatory adversaries

1. collision changes winding but geometry remains trapped;
2. geometry expands in three directions because a hidden threshold names
   three;
3. a direct rank reset disguised as a release mark;
4. finite-width full-support rank concentration mislabeled exact selection;
5. isotropic permutation symmetry mislabeled continuous rotation symmetry;
6. quotient-only observability with a hidden pre-quotient mode;
7. different generators used for attraction and departure;
8. retarded boundary data smuggled in and then called dynamically selected;
9. a response mode with zero residue;
10. an apparent three-dimensional plateau that disappears under one
    predeclared scale/error perturbation.

## Required conclusion

End with exactly one of:

1. a source-authorized minimal hybrid plus a theorem and executable finite
   control that can now be implemented; or
2. a sharp obstruction showing which additional action/port term is logically
   indispensable and how it can be measured without encoding three.

Classify every substantive statement as `primary-source derived`, `derived
from a newly proposed action`, `exact theorem about a declared model`,
`controlled asymptotic/numerical conjecture`, `no-go/underdetermination`, or
`open gate`.

## Delivery contract

Write the full answer to
`responses/0009_source_authorized_winding_geometry_hybrid.md`, then commit and
push it directly to `main`.  Do not open a pull request, do not offer a
download, and do not leave the result only in chat.
