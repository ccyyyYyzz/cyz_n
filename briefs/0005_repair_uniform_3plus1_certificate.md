# Brief 0005 — Repair the uniform visible 3+1 theorem at its three hard interfaces

Status: active

## Role

Act as the originating mathematical physicist. This is not a request to
review response 0004. Produce a corrected replacement theorem/addendum that
keeps its useful architecture while repairing the logical interfaces below.

Read, in order:

1. `state/PROJECT_STATE.md`;
2. `decisions/0004_adopt_regular_branch_lift.md`;
3. `briefs/0004_end_to_end_3plus1_certificate.md`;
4. `responses/0004_end_to_end_3plus1_certificate.md`;
5. this brief.

## What is retained from response 0004

Retain and sharpen:

- the compact gauge-quotiented all-candidate viewpoint;
- the finite affine/filtered presentation-orbit witness;
- the selected top-\(r\) right-singular-subspace theorem;
- the strict gate ledger;
- the distinction between tangent chart overlaps and connection transport;
- the directed \(\mathbb Z_2\) no-flip criterion;
- the anonymous four-dimensional arithmetic control and adversaries.

Do not merely restate these pieces. Repair their composition.

## Blocking repair 1 — candidate truth is not identifiability

Response 0004 puts completeness and successful identification certificates
inside the candidate tuple and then defines the target set by those same
certificates. This makes missing panel points, rank-eight Green data and absent
coverage look like target exclusion, although they are evidence loss.

Define the raw fiber instead as

\[
\mathfrak C_D=
\{C:\operatorname{Pred}(C)\in D,
\ C\text{ obeys only the predeclared complexity/height/order bounds}\}/\mathcal G.
\]

The class must not fix dimension four, regularity, Lorentz signature, a
relation ideal, a cone, time orientation, context closure, panel unisolvence or
physical coverage. Completeness evidence must not determine membership in
\(\mathfrak C_D\).

Separate three notions:

1. \(\mathsf T(C)\): the candidate actually has the target physical
   four-dimensional, Lorentzian, universal-cone, overlap-glued and
   time-orientable structure;
2. \(\mathsf R(C,D)\): the registered reconstruction route succeeds for that
   candidate and data;
3. external predicates \(\mathbf C_{\rm ctx},\mathbf C_{\rm rel},
   \mathbf C_{\rm phys}\), each with its own scope and proof artifact.

Give a non-vacuous three-verdict theorem with these rules:

- certification requires a nonempty raw fiber, all required completeness
  predicates, \(\mathsf R\) and \(\mathsf T\) for every candidate, and one
  registered full-structure orbit;
- target exclusion requires a nonempty raw fiber and a robust obstruction to
  \(\mathsf T\) for every candidate; failure of \(\mathsf R\), a missing
  completeness artifact, rank eight, an incomplete panel or a closed margin
  is never by itself such an obstruction;
- all other cases are inconclusive;
- an empty raw fiber is reported separately as incompatibility of the declared
  model class with the data, not as evidence that the world is non-3+1.

State exact target-obstruction witnesses separately from failed-identification
witnesses. Re-run the missing-panel, delayed-context and rank-eight adversaries
under the corrected semantics.

## Blocking repair 2 — actually solder Green data to the principal co-metric

The corrected theorem must explicitly define

\[
T_x^*B_0=\mathfrak m_x/\mathfrak m_x^2,
\qquad
K_x=\mathfrak m_x^2/\mathfrak m_x^3,
\]

and the regular multiplication isomorphism

\[
\mu_{2,x}:\operatorname{Sym}^2T_x^*B_0
\xrightarrow{\sim}K_x,
\qquad [f]\odot[g]\mapsto[fg].
\]

For

\[
Z_x=\ker(\pi_xU_x),\qquad
H_x=U_x|_{Z_x}:Z_x\to K_x,\qquad
\beta_x=B_x|_{Z_x},
\]

derive from the reduced Green equation, rather than assume,

\[
H_x^\vee(p_{L,x})=p_{L,x}\circ H_x=\beta_x.
\]

Use the algebraic dual, not a Gram-dependent Hilbert adjoint. Centered sources
give \(\beta_x=0\). Only then may rank nine in a ten-dimensional \(K_x\), plus
nonzero second-order propagation evidence, identify

\[
\mathcal L_x
=\mu_{2,x}^\vee\!\left(\ker H_x^\vee\right)
\subset \operatorname{Sym}^2T_xB_0.
\]

Make every presentation, local-jet and overlap isomorphism intertwine
\(\mu_2\). Specify the coordinate convention for the raw symmetric-product
basis versus Frobenius-orthonormal `svec`, including the off-diagonal
\(\sqrt2\) factors. Then state the correct perturbation-radius propagation.

## Blocking repair 3 — one fixed scalar cone and every feasible field factor

For each actual matrix-valued principal symbol, require the fixed Green line,
not merely flattening rank one:

\[
\mathsf P=q\otimes K,
\qquad
\|(I-\Pi_q)\operatorname{Flat}(\mathsf P)\|=0.
\]

Quantify over every declared field polarization, nuisance-compatible symbol and
interval-consistent candidate. Certification additionally requires

\[
\inf_{\mathsf P\text{ feasible}}\sigma_{\min}(K(\mathsf P))>0.
\]

A nominal invertible \(K\) is insufficient when the response fiber contains a
singular \(K'\). Give either:

- an exact necessary-and-sufficient finite condition for uniform invertibility
  over the bounded feasible field-factor set; or
- a sharp sufficient certificate with an explicit compact feasible set and a
  positive lower bound, while labeling the remaining global optimization as
  architecture.

The exact positive control must use measurements/class bounds that genuinely
exclude singular feasible factors. The annihilator design whose kernel is the
entire \(q\otimes\operatorname{End}(F)\) is not a positive universal-
invertibility control unless an independent compact restriction separates all
singular factors.

Field/polarization completeness is an independent scoped premise. It cannot be
inferred from observing two named channels.

## Tensor, overlap and time requirements

Treat \(Q\) as the principal co-metric. If tangent components transform by
\(v_\beta=J_{\beta\alpha}v_\alpha\), require

\[
Q_\beta=\omega J_{\beta\alpha}Q_\alpha J_{\beta\alpha}^{\top},
\qquad
g_\beta=Q_\beta^{-1}
=\omega^{-1}J_{\beta\alpha}^{-\top}g_\alpha
J_{\beta\alpha}^{-1}.
\]

Directed tangent vectors are classified with \(g\), not directly with \(Q\).
The atlas certificate must cover every physical tangent overlap and check the
full tangent and induced symmetric-square cocycles on every declared triple
overlap. Omitting one overlap edge, or checking only the principal ray, must
force abstention. A connection remains outside this cocycle.

## Close the finite witness claim

For the affine/filtered presentation witness, add the missing finite data:

- a polynomial inverse variable \(C\) with \(AC=CA=I\);
- degree and coefficient-height bounds for ideal-membership multipliers;
- explicit context intertwiner, unit, character and calibrated-Gram equations;
- finite cover-incidence, local-jet, \(\Theta\) and \(\mu_2\) intertwining
  equations;
- compact bounds for every minimization variable;
- both forward and reverse ideal containments.

If a Positivstellensatz or effective membership-height bound is only assumed,
state it as an independent class bound. Do not call the global optimizer
implemented.

## Required output

Produce one self-contained response with:

1. corrected raw candidate fiber and logical truth table;
2. corrected Green--\(\mu_2\)--principal theorem and proof;
3. corrected fixed-\(q\), all-feasible-field theorem;
4. complete overlap/time cocycle theorem;
5. repaired finite presentation witness;
6. a corrected anonymous exact positive control;
7. the key adversaries rerun under the new verdict semantics;
8. a minimal Paper-I theorem;
9. a short table with exactly three evidence labels:
   `exact proved`, `conditional exact`, and `implementation architecture`.

Do not claim that physical coverage, compact candidate enumeration, global
semialgebraic emptiness, interval optimization or all-feasible-factor
minimization has been implemented unless you provide the actual finite proof
artifact or algorithm and its termination assumptions.

## Delivery — write directly to GitHub

Write the completed response directly to `ccyyyYyzz/cyz_n` as

`responses/0005_repaired_uniform_3plus1_certificate.md`.

Commit to `main` if permitted. Otherwise create branch `gpt/brief-0005` and
open a pull request. Do not overwrite response 0004, do not edit state or
decisions, and do not return only a downloadable attachment.

## Decision criterion

The repair succeeds only if evidence loss and target falsity are logically
separate, the Green relation reaches the actual principal co-metric through
\(\mu_2^\vee\), and common-cone certification quantifies over every feasible
field factor rather than one nominal matrix.
