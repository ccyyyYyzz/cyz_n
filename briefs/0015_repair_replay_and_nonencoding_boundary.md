# Brief 0015 — repair replay and close the constructor/non-encoding boundary

Status: active

Baseline response commit: `d23e68de88f27a6896feb6631ea0293a5cdf90f3`.

## Role

Act as the originating mathematical physicist and computational co-theorist.
This is a repair and theorem-boundary round, not an invitation to replace the
kernel with a new mechanism or to defend Response 0014.

Read, in order:

1. `state/PROJECT_STATE.md`;
2. `responses/0014_close_framewise_kernel_with_hostile_verifier.md`;
3. `decisions/0014_adopt_generator_reject_broken_verifier.md`;
4. this brief.

Preserve the independently replayed generator-level closure.  Do not claim
rank-three selection or exclusion.

## Exact defects to repair

The current generator deterministically produces the claimed frame-local,
reverse-closed scheduled kernels.  The committed verifier cannot decode its
source shards and exits before parsing arguments.  The artifact hashes the
thin launcher but not the shards, so the kernel and mutation report remain
apparently self-consistent while their independent verifier is unusable.

The registered dependency graph also covers only part of the constructor.
Frame enumeration, fixed frame arity, state/atom schemas, probability
composition, reverse and ledger rules, initial law, age rule and control
instances remain in free Python.  A semantic non-encoding certificate cannot
quantify over only the declarative half.

Finally, source-invalid exits change physical projection and energy while
declaring `physical_projection_changed=false`, and the phrase
`proposed_microcanonical_reverse` is not supported by a serialized
density-of-states measure or detailed-balance identity.

## Required delivery

Commit under new `0015` paths:

1. `responses/0015_repair_replay_and_nonencoding_boundary.md`;
2. readable standard-Python generator and independent verifier sources under
   `artifacts/0015/`;
3. a compact canonical kernel/specification artifact;
4. a newly computed baseline, portability and hostile-mutation report; and
5. scoped `.gitattributes` entries if they are needed to make checked-out
   script and JSON identities unambiguous.

The verifier must import no generator module or generator helper.  Verifier
modules may import other plainly readable verifier-only modules.  Do not hide
the implementation in base85, compressed strings, generated source shards or
another opaque transport wrapper.  Split normal `.py` modules at semantic
boundaries if a single file is too large.

Do not create a `contents: write` workflow.  Write, commit and push ordinary
repository files directly to `main`.

## Preserve and re-establish the adopted kernel facts

The repaired delivery must independently reproduce:

- full `T9`: 21,172 states, 6,556 total marks of which 6,552 are frame-local,
  78,628 atoms and 39,312 positive non-null atoms;
- three-large and four-large complete kernels through the same constructor;
- one exact row and one schedule per state;
- exact row normalization;
- frame-local `f013` marks;
- reverse endpoint/mark/channel/ledger closure;
- deterministic event times `2,4,6,8,10`; and
- all eight full-process adjacent `S9` actions.

If repairing cemetery semantics changes state or atom counts, report both the
old and new counts and explain the exact bijection or lift.  Do not preserve a
wrong count merely to match Response 0014.

## A complete registered constructor specification

Serialize and hash one finite constructor specification that contains every
ordinary kernel dependency:

1. direction set and role-labelled frame arity/enumeration;
2. history and cemetery alphabets;
3. frozen microscopic input schema;
4. validity predicates and their actual parameter semantics;
5. mark construction and probabilities;
6. state, schedule, channel and event schemas;
7. probability composition;
8. destination, history/reset and reverse rules;
9. energy, work and charge ledgers;
10. initial and residual-age laws;
11. adjacent permutation actions; and
12. the scheduled Markov-renewal process type and CTMC-export prohibition.

The generator and verifier must be independent interpreters of this serialized
specification.  Their implementation code is not itself the specification,
but neither interpreter may introduce a constitutive branch, constant or
lookup table absent from it.  Every declared parameter such as precedence,
closed/open interval semantics and frame arity must actually be read by both
interpreters.

Hash the complete specification.  Record that hash in every generated
instance.  The artifact and report must bind the normalized hash of every
executable `.py` dependency, not merely a launcher.  The verifier must reject
addition, deletion or substitution of one dependency.

Serialize a separate `control_vector_manifest` containing the ordinary, d3,
d4 and arity-2/4 exact inputs and expected replay obligations.  Hash the
constructor specification and the control manifest separately, and bind both
hashes in the report.  Every instance must share the constructor-specification
hash; control vectors are tests of the law, not part of the law.

## Arbitrary metric input and opaque controls

Expose a documented CLI or machine API accepting one exact nine-component
metric plus the other whitelisted microscopic inputs.  It may not accept a
rank, visible count, active mask or named stratum.

Use opaque instance IDs for the ordinary and control runs.  Human-readable
labels may live only in report metadata that the constructor never reads.
Re-run the three-large and four-large metrics through this generic interface.

## Frame arity is an explicit prior, not a dimension result

The ordered `(w,v,s)` interaction frame has arity three.  Do one of:

1. derive those three roles from the cited collision/recollision source model
   without using a desired visible dimension; or
2. label frame arity as a proposed microscopic closure parameter.

In either case, add arity-2 and arity-4 hostile/control specifications using
the same interpreter.  The verifier must show that it reads the registered
arity rather than a hard-coded triple.  Report how valid-frame onset and
kernel counts change.

These controls do not need to preserve the physical GKM interpretation.  They
are semantic adversaries proving that the software certificate does not
silently equate interaction-role arity with visible response rank.  Unless a
source derivation is supplied, the theorem boundary must say:

```text
target-rank-free conditional on a declared arity-three microscopic closure;
not a fundamental non-encoding or three-selection theorem
```

## Projection-preserving killed exits

Replace the current invalid transition that mimics annihilation.  Prefer an
absorbing killed-state lift that retains the source state's physical
projection, system/reservoir/work energies, charge vector, frame and history,
while adding only a source-validity status and cemetery reason.

The exit atom must have zero physical ledger, must not claim a physical
projection change, and must be explicitly exempt from reverse dynamics as an
epistemic domain exit.  The killed state must be absorbing under a declared
schedule exemption.  Mutating any preserved payload field, ledger entry,
reason or absorbing row must fail.

Under every adjacent `S9` action, a killed state must follow its source
frame/state, its reason must remain invariant or transform by a declared
action, its preserved physical payload must transform covariantly, and its
absorbing row and schedule exemption must map to the corresponding killed
row.

If a different construction is chosen, prove its semantics with the same
precision; a boolean exemption alone is not enough.

## Reverse terminology

Rename `proposed_microcanonical_reverse` to a neutral phrase such as
`proposed_reverse_ratio` unless the artifact serializes:

- the reference measure or density of states;
- all degeneracy factors;
- the exact forward and reverse fluxes; and
- an independently checked detailed-balance identity.

A ratio of `1/4` by itself is not a microcanonical theorem.

## Independent verifier requirements

From the compact artifact alone, the verifier must independently:

1. validate the full constructor schema and operation parameter schemas;
2. expand every state, mark, schedule, atom, row and killed state;
3. recompute all canonical expanded hashes and counts;
4. traverse row normalization, destinations, reverse IDs and ledgers;
5. replay exact residual-age trajectories;
6. derive and check the complete `S9` actions and the serialized adjacent
   generator list;
7. rebuild ordinary, d3, d4 and arity-2/4 controls;
8. validate the fixed initial law; and
9. reject hidden target/rank dependencies anywhere in the full constructor
   specification.

The artifact process type must be exactly `scheduled_markov_renewal`.  The
verifier must reject `row_generator`, `ctmc_generator`, `rate_matrix` and any
attempt to replace a deterministic hold by an exponential rate.  No CTMC
export is permitted in this round.

The generator may be invoked only as an external byte/canonical-payload
regeneration control.  Independent semantic success may not depend on
importing or executing generator code.

## Hostile mutations

Retain all 22 mutations from Brief 0014 and add independent mutations for:

1. one executable dependency file or dependency hash;
2. frame arity changed without regenerating the kernel;
3. a constitutive branch inserted outside the registered specification;
4. a hard-coded triple in either interpreter while the spec says arity two or
   four;
5. an opaque instance ID renamed to contain `three` or `four`: first verify
   that the kernel is unchanged up to ID isomorphism, then inject a branch on
   that label and require rejection;
6. killed-state physical payload, ledger, reason and absorbing row;
7. serialized `s9_adjacent_generators` removed or corrupted;
8. a declared parameter (`precedence` or interval closure) changed while an
   interpreter ignores it; and
9. a proposed reverse ratio relabelled as detailed balance without a measure.

Each mutation must run in an isolated child process, return nonzero and give a
localized semantic code.  The report must be generated from those observed
child results.

## Clean-tree and cross-platform replay

The final committed tree must satisfy, from a fresh temporary copy or archive:

```text
python -m py_compile <every delivered Python module>
python artifacts/0015/generate_0015.py ...
python artifacts/0015/verify_0015.py --baseline ...
python artifacts/0015/verify_0015.py --portability ...
python artifacts/0015/verify_0015.py --report ...
```

All commands must exit zero.  A `--help` invocation must also exit zero.

Do not compare raw checkout bytes when line-ending conversion is irrelevant.
Use canonical parsed JSON payload hashes and LF-normalized source hashes, or
force scoped LF through repository attributes.  Demonstrate both LF and CRLF
copies.  The final report must list the actual hash of every source file it
executed.

## Required theorem boundary

End with exactly the strongest supported result:

1. a replayable generator-independent certificate for a complete registered,
   target-rank-free scheduled constructor, explicitly conditional on any
   proposed frame-arity prior; or
2. a sharp obstruction identifying the smallest remaining unregistered rule
   or physical derivation.

Do not claim visible dimension, Lorentzian signature, rank-three selection,
detailed balance or a faithful continuous GKM cubature in this round.
