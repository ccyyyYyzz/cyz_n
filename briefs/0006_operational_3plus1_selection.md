# Brief 0006 — Build the selection bridge for one closed-loop (3+1) result

Status: active

## Role

Act as the originating mathematical physicist. Do not review response 0005 and
do not discuss journals. The reconstruction architecture has survived. Now
originate the next theorem and mechanism that can turn it into an explanation
of why visible \(3+1\) is stable and selected.

Read, in order:

1. `state/PROJECT_STATE.md`;
2. `decisions/0005_adopt_repaired_uniform_certificate.md`;
3. `responses/0005_repaired_uniform_3plus1_certificate.md`;
4. this brief.

## New program constraint

The work will not be split into Paper I/II/III. The only final object is one
closed chain:

\[
\text{anonymous responses}
\to \text{visible branch and cotangent}
\to \text{Lorentz common cone and time component}
\to \text{scale-stable }3+1\text{ basin}
\to \text{falsifiable departure}.
\]

The former layers are dependency packages, not publication units.

## Hard conceptual boundary

Do not identify selection with any of the following:

- a spectral, Hausdorff or graph-growth dimension numerically approaching 4;
- a four-dimensional triangulation or hypercuboid model becoming extended;
- shrinking extra dimensions around a base whose dimension was fixed to four;
- a Hamiltonian whose preferred valence or loop terms already encode a cubic
  three-dimensional lattice;
- one tuned cosmological history without an open perturbation basin;
- a mechanism that selects three large spatial directions but never proves
  that restricted responses recover a one-time Lorentzian universal cone.

Every candidate mechanism must output the same operational package recovered
by response 0005.

## Required construction

### 1. Scale-indexed operational object

Define a filtration of intervention algebras or coarse-response maps and a
functorial scale-indexed package

\[
\mathcal O_\lambda=(B_\lambda,d_\lambda,[Q_\lambda],
\mathcal N_\lambda,\mathcal F_\lambda,
\mathfrak t_\lambda;\mathbf m_\lambda).
\]

Here \(\mathbf m_\lambda\) must contain the actual uniform reconstruction,
signature, all-feasible-field, overlap and time margins. State precisely when
maps between scales preserve or reveal cotangent directions. Do not assume
monotonicity of visible dimension without proving the needed smoothness and
old-direction injectivity.

### 2. Plateau and attraction theorem

Give an exact theorem defining a robust visible \(3+1\) plateau on a nonzero
scale interval and a genuine attractive basin under a microscopic or
coarse-intervention semigroup. It must distinguish:

1. visibility/hiding;
2. stability of an already chosen four-dimensional quotient;
3. selection from a state space containing resolved non-\(3+1\) alternatives.

Use either a Lyapunov/contraction theorem with explicit margins or a precise
probabilistic concentration theorem. State the open-basin and non-encoding
hypotheses. Compose it with the all-candidate reconstruction theorem without
putting completeness or target truth inside the raw fiber.

### 3. Mechanism decision

Compare exactly these three routes through that theorem:

- compact-radius/Kaluza--Klein flow as a mandatory negative control;
- Brandenberger--Vafa winding-string decompactification, especially
  arXiv:0908.0955 and arXiv:1212.2115;
- dimension-variable graph dynamics in the Quantum Graphity/combinatorial
  gravity family, beginning with Phys. Rev. D 77, 104029 and
  arXiv:1811.12905.

For each, identify what is input, what is dynamically selected, whether one
time and a common cone follow, and whether an observable scale departure is
available. Then choose the strongest primary mechanism for the next technical
cycle, or prove that none yet passes and specify the smallest new model that
would.

### 4. Minimal executable model

Provide equations and a finite computational experiment for the chosen route.
It must include:

- a state space with at least one resolved non-\(3+1\) alternative;
- a perturbation family rather than one trajectory;
- a measurable response interface from which the existing reconstruction
  package could in principle be computed;
- a quantitative attraction or failure criterion;
- one adversary showing that a scalar dimension estimator can look correct
  while the common-cone/time package fails.

Label any phenomenological rate or closure approximation honestly. Do not
invent parameters absent from the cited dynamics.

### 5. Independent dimension witness

Explain how nonlinear characteristic-front interaction order can serve as an
independent check of \(d_\lambda\), rather than being inferred from the same
quadratic Green ansatz. State the minimum theorem needed at the interface; do
not claim the microlocal result is already proved.

### 6. Falsifiable closure

Derive at least one response-level prediction of where the \(3+1\) certificate
must become inconclusive or fail as bandwidth, source profile, scale or state
preparation changes. It must use the same mechanism parameters responsible for
the attractive plateau.

## Required output

Produce one self-contained response with:

1. definitions of the scale package, plateau and basin;
2. a composition theorem and proof;
3. the three-mechanism comparison;
4. one mechanism decision;
5. a minimal executable model and adversaries;
6. the nonlinear interaction-order interface;
7. a falsifiable prediction;
8. an evidence table using exactly `exact proved`, `conditional exact`, and
   `implementation architecture`;
9. a concrete next-30-day kill plan.

## Delivery — write directly to GitHub

Write the completed response directly to `ccyyyYyzz/cyz_n` as

`responses/0006_operational_3plus1_selection.md`.

Commit to `main` if permitted. Otherwise create branch `gpt/brief-0006` and
open a pull request. Do not edit response 0005, state or decisions, and do not
return only a downloadable attachment.

## Decision criterion

The response succeeds only if four is not already encoded in the mechanism's
state space, the attractor concerns the full response-derived Lorentz/common-
cone/time package rather than a scalar dimension, and the same mechanism gives
a departure that could in principle falsify it.
